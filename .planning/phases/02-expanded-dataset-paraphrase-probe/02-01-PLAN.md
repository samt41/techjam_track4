---
phase: 02-expanded-dataset-paraphrase-probe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - arena/evaluator_bridge.py
  - tests/test_arena_boundary.py
  - arena/datasets/__init__.py
  - starter/shopping_agent/constraint_extractor.py
  - tests/test_constraint_extractor.py
  - .gitignore
autonomous: true
requirements: [MEAS-10, MEAS-11, MEAS-12]

must_haves:
  truths:
    - "The evaluator seam re-exports exactly eight names and the AST guard asserts that count in the same commit (D-47)"
    - "The AST boundary scan recurses into arena/datasets/ and fires on a nested violation (D-47, L-1)"
    - "A nested arena/datasets/evaluator_bridge.py cannot be silently exempted from the scan (L-1)"
    - "The D-34 divergence gate can import a public STOPWORDS without reaching for a private name (D-54)"
    - "A single gitignored .scratch/ root exists, so every operator smoke-run and byte-comparison command in this phase pins an explicit repo-relative path instead of a POSIX-only $TMPDIR that expands to empty on the documented Windows/PowerShell platform"
  artifacts:
    - path: "arena/evaluator_bridge.py"
      provides: "Eight-name evaluator seam with a commented why per added name"
      contains: "materialize_hidden_fields"
    - path: "arena/datasets/__init__.py"
      provides: "Package stub that makes the recursive boundary scan non-vacuous"
    - path: "starter/shopping_agent/constraint_extractor.py"
      provides: "Public STOPWORDS constant"
      contains: "STOPWORDS"
  key_links:
    - from: "tests/test_arena_boundary.py"
      to: "arena/datasets/"
      via: "rglob over the arena package"
      pattern: "rglob"
    - from: "arena/evaluator_bridge.py"
      to: "evaluator.local_evaluator"
      via: "one from-import of exactly eight names"
      pattern: "from evaluator.local_evaluator import behavior_for"
---

<objective>
Widen the sole evaluator seam from three names to eight (D-47) and widen its machine-checked
guard in the same commit, so every evaluator function this phase consumes arrives through one
reviewable line and the D-08/MEAS-15 boundary stays enforced rather than quietly loosened.
Create the `arena/datasets/` package stub in the same plan so the newly-recursive scan has a
real nested module to walk, and promote `constraint_extractor._STOPWORDS` to public `STOPWORDS`
(D-54) so the D-34 divergence gate in plan 02-05 does not import a private name across packages.

Purpose: every subsequent plan in this phase reaches `intent_card`, `behavior_for`,
`classify_constraint`, `materialize_hidden_fields` and `searchable_text` through this seam. Until
the seam and its guard widen together, nothing in `arena/datasets/` can be written without
breaching a hard invariant, and `arena/datasets/*.py` would be entirely unguarded because
`tests/test_arena_boundary.py:81` uses non-recursive `glob("*.py")` (L-1).

Output: an eight-name seam, a recursive path-anchored boundary scan with a proof case, an empty
`arena/datasets/` package, and a public `STOPWORDS`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-expanded-dataset-paraphrase-probe/02-CONTEXT.md
@.planning/phases/02-expanded-dataset-paraphrase-probe/02-PATTERNS.md
@CLAUDE.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen the seam to eight names and make its guard recursive and path-anchored (D-47, L-1)</name>
  <files>arena/evaluator_bridge.py, tests/test_arena_boundary.py, arena/datasets/__init__.py</files>
  <read_first>
    - `arena/evaluator_bridge.py` (all 17 lines — the whole file is the pattern)
    - `tests/test_arena_boundary.py` (all 167 lines; lines 17, 19, 77-83, 98-132, 134-141, 151-163 are the ones that change or must NOT change)
    - `evaluator/local_evaluator.py` lines 27-37 (`searchable_text`), 52-71 (`intent_card`), 74-87 (`behavior_for`), 137-151 (`classify_constraint`), 204-213 (`materialize_hidden_fields`) — read them to write the per-name *why* comments accurately
    - `.planning/phases/02-expanded-dataset-paraphrase-probe/02-PATTERNS.md` § "`arena/evaluator_bridge.py` + `tests/test_arena_boundary.py` (the pair, one commit)"
    - `arena/__init__.py` (the one-line package-stub precedent)
    - `arena/metrics.py` lines 1-15 (the transcription note that must be repeated at the seam)
  </read_first>
  <action>
    All three files change in ONE commit — D-47 requires the seam and its guard to move together so
    the invariant is never briefly unenforced.

    In `arena/evaluator_bridge.py`:
    - Replace the single import at line 14 with `from evaluator.local_evaluator import behavior_for, catalog_index, classify_constraint, evaluate, intent_card, load_jsonl, materialize_hidden_fields, searchable_text` — alphabetical, because the guard compares against `sorted(alias.name for alias in seams[0].names)`.
    - Set `__all__` to the same eight names, sorted: `("behavior_for", "catalog_index", "classify_constraint", "evaluate", "intent_card", "load_jsonl", "materialize_hidden_fields", "searchable_text")`.
    - Rewrite the docstring: change "exactly the three names below" to "exactly the eight names below", and add a *why* for each of the five added names (D-47 requires the why at the seam): `intent_card` builds the control arm verbatim (D-31); `behavior_for` is the fallback behavior the control arm is compared against (D-55); `classify_constraint` is the only authority on which question unlocks a constraint (D-33/F-05); `materialize_hidden_fields` proves branch 1 fired (D-37); `searchable_text` is the exact six-field concatenation the D-34 overlap gate measures against.
    - Add the sentence from `arena/metrics.py`'s transcription note: widening the seam must NOT tempt anyone to replace `arena/metrics.py` with evaluator imports, because the cross-agreement between the two independent metric paths is the MEAS-16 validation evidence.
    - Add NO function and NO class. The guard asserts zero `FunctionDef`/`AsyncFunctionDef`/`ClassDef`; any adapter belongs in `arena/datasets/`.

    In `tests/test_arena_boundary.py`:
    - Update `BRIDGE_EXPORTS` (line 17) to the same sorted eight-name tuple.
    - Replace `_BRIDGE_MODULE_NAME = "evaluator_bridge.py"` usage in `_non_bridge_modules` with a path-anchored exemption. Keep the constant for the `test_bridge_surface...` lookup, and add `_BRIDGE_RELATIVE_PATH = Path("arena") / _BRIDGE_MODULE_NAME`.
    - Rewrite `_non_bridge_modules` (lines 77-83): `arena_directory.rglob("*.py")`, skip any path with `"__pycache__"` in `path.parts`, and exempt via `path.relative_to(REPOSITORY_ROOT) != _BRIDGE_RELATIVE_PATH` — NOT `path.name != _BRIDGE_MODULE_NAME`. Comment the why: under `rglob` a basename comparison would silently exempt a second, unguarded `arena/datasets/evaluator_bridge.py` (L-1).
    - Rename `test_bridge_surface_is_exactly_three_names` to `test_bridge_surface_is_exactly_the_declared_names` and change the failure message "a fourth name through the seam..." to "a ninth name through the seam breaks 'evaluate() as an opaque function'".
    - Add `test_scan_reaches_a_nested_module` to `ScannerTest`, mirroring the `_scan` discipline at lines 52-56: build `<tmp>/arena/datasets/probe.py` containing `import evaluator.local_evaluator`, run the same collection predicate used by `_non_bridge_modules` (factor it into a module-level helper `arena_modules(root: Path) -> list[Path]` taking a root so both the live scan and the temp-dir proof share one implementation), and assert the nested file is collected AND that `evaluator_references` fires on it.
    - Add `test_a_nested_bridge_named_module_is_not_exempt`: write `<tmp>/arena/datasets/evaluator_bridge.py` with `import evaluator.local_evaluator` and assert `arena_modules(<tmp>)` still contains it. This is the two-sided half of the L-1 fix.
    - Extend `test_arena_package_has_modules_to_scan` so it also asserts at least one collected path lies under `arena/datasets/` — otherwise the recursion is unproven against the real tree.
    - Do NOT touch `EVALUATOR_SHA256` or `EvaluatorIntegrityTest`.

    Create `arena/datasets/__init__.py` as a stub exporting nothing, matching `arena/__init__.py`'s
    shape, with a one-line docstring naming the package's job (offline corpus generation and
    freezing, D-43) so the recursive scan has a real nested module and
    `test_arena_package_has_modules_to_scan` is non-vacuous.

    Append one line to `.gitignore`, placed with the other working-directory excludes and above the
    `data/` block: `.scratch/`, with a comment stating that it is the repo-relative scratch root every
    operator command in phase 2 writes to. The reason it must exist before plan 02-04 runs: this
    repository documents Windows 11 / PowerShell as its platform, where `$TMPDIR` expands to the empty
    string and a command like `--response-log "$TMPDIR/probe-smoke.jsonl"` silently writes to the drive
    root. Pinning one relative path removes the shell dependency entirely and keeps the smoke artifacts
    out of `git status`.
  </action>
  <verify>
    <automated>uv run python -m unittest tests.test_arena_boundary -v</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -m unittest tests.test_arena_boundary -v` exits 0.
    - `grep -c "rglob" tests/test_arena_boundary.py` is ≥ 1, and `grep -c 'arena_directory.glob("\*.py")' tests/test_arena_boundary.py` is 0.
    - `grep -v '^\s*#' arena/evaluator_bridge.py | grep -c "materialize_hidden_fields"` is ≥ 2 (the import line and `__all__`).
    - `uv run python -c "import arena.evaluator_bridge as b; assert b.__all__ == ('behavior_for','catalog_index','classify_constraint','evaluate','intent_card','load_jsonl','materialize_hidden_fields','searchable_text'), b.__all__"` exits 0.
    - Two-sided recursion proof: `test_scan_reaches_a_nested_module` fails if `rglob` is reverted to `glob` — verify by running the new test against a temporarily reverted `arena_modules` helper in a scratch copy, or equivalently confirm the test asserts on a path containing `datasets` rather than only on a non-empty list.
    - `test_a_nested_bridge_named_module_is_not_exempt` collects `arena/datasets/evaluator_bridge.py`, proving the exemption is path-anchored and not basename-anchored.
    - `arena/datasets/__init__.py` exists and `uv run python -c "import ast,pathlib; t=ast.parse(pathlib.Path('arena/datasets/__init__.py').read_text()); assert not [n for n in ast.walk(t) if isinstance(n,(ast.FunctionDef,ast.ClassDef))]"` exits 0.
    - `uv run python -m unittest` full suite exits 0 (the seam change must not break any Phase 1 test).
    - `git check-ignore -v .scratch/probe-smoke.jsonl` exits 0 (the path IS ignored) while
      `git check-ignore -v data/datasets.json` exits non-zero (it is NOT) — both directions, so the new
      rule is proven narrow rather than merely present.
  </acceptance_criteria>
  <done>A gitignored `.scratch/` root exists; the seam re-exports eight names with a per-name why; the AST scan recurses into `arena/datasets/`, path-anchors the bridge exemption, and has both a positive nested-reach case and a negative nested-bridge case; `EVALUATOR_SHA256` is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Promote `_STOPWORDS` to public `STOPWORDS` (D-54)</name>
  <files>starter/shopping_agent/constraint_extractor.py, tests/test_constraint_extractor.py</files>
  <read_first>
    - `starter/shopping_agent/constraint_extractor.py` lines 69-141 (the comment block at 75-78, the frozenset at 79-94, the single call site at 109)
    - `starter/shopping_agent/text_normalization.py` lines 1-15 (the public `UPPER_SNAKE` constant precedent: `WHITESPACE_RE`, `TOKEN_RE`, `PUNCT_SPACING_RE` carry no underscore)
    - `.planning/codebase/CONVENTIONS.md` lines 42-52 (public constants carry no underscore; tuning constants are underscore-prefixed)
    - `tests/test_constraint_extractor.py` (check whether the private name is referenced anywhere in tests before renaming)
  </read_first>
  <action>
    Rename `_STOPWORDS` to `STOPWORDS` in `starter/shopping_agent/constraint_extractor.py` and update
    the single call site at line 109 (`if phrase in _STOPWORDS:`) in the same commit. Grep the whole
    repository for `_STOPWORDS` first and update every hit; there is exactly one non-definition hit
    today, but verify rather than assume.

    Extend — do not replace — the existing comment above the frozenset (currently at lines 75-78,
    explaining that the list is generic, carries no garment vocabulary, and needs no
    evaluator-specific tuning). Add one sentence naming the new cross-package consumer: the D-34
    lexical-divergence gate in `arena/datasets/divergence.py` measures probe-phrase content tokens
    after removing these words, so one list serves both the gazetteer and the gate. Also note that
    the list contains `"no"` and `"not"`, so negation is invisible to a *lexical* gate by design and
    negation drift is caught by the D-35 faithfulness review instead.

    The constant carries no leading underscore because it is now public API for another package
    (CONVENTIONS.md line 44). Do not add a `_STOPWORDS = STOPWORDS` alias — a private name importable
    across packages is the worse precedent D-54 rejects.

    If `tests/test_constraint_extractor.py` references the private name, update it; otherwise add one
    test asserting `STOPWORDS` is a `frozenset`, is non-empty, contains `"the"` and `"no"`, and does
    NOT contain `"buckle"` or `"dress"` (the two words the existing comment names as the reason a
    catalog-derived stop list was rejected).
  </action>
  <verify>
    <automated>uv run python -m unittest tests.test_constraint_extractor -v</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -m unittest tests.test_constraint_extractor -v` exits 0.
    - `uv run python -c "from starter.shopping_agent.constraint_extractor import STOPWORDS; assert isinstance(STOPWORDS, frozenset); assert 'the' in STOPWORDS and 'no' in STOPWORDS; assert 'buckle' not in STOPWORDS and 'dress' not in STOPWORDS"` exits 0.
    - `grep -rn "_STOPWORDS" starter/ arena/ tests/ experiments/` returns no matches (two-sided: the rename is complete, not merely additive).
    - `grep -v '^\s*#' starter/shopping_agent/constraint_extractor.py | grep -c "STOPWORDS"` is ≥ 2 (definition plus call site).
    - `uv run python -m unittest` full suite exits 0.
  </acceptance_criteria>
  <done>`STOPWORDS` is public, the private name is gone repository-wide, the existing rationale comment names the D-34 consumer and the negation caveat, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `arena/` → `evaluator/` | The only permitted crossing is `arena/evaluator_bridge.py`; this plan widens it |
| `arena/datasets/` (new) → `arena/evaluator_bridge.py` | A new subpackage that the pre-existing AST scan did not reach |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering | `evaluator/local_evaluator.py` | accept | Already machine-checked: `EvaluatorIntegrityTest` (`tests/test_arena_boundary.py:151-163`) pins the byte digest. This plan does not touch the evaluator or the constant |
| T-02-12 | Tampering | `arena/datasets/*.py` importing the evaluator directly, bypassing the seam | mitigate | Convert `_non_bridge_modules` from `glob` to `rglob`, path-anchor the bridge exemption, and add both a positive nested-reach case and a negative nested-bridge case (L-1) |
| T-02-13 | Elevation of Privilege | An adapter or wrapper added to the seam, turning a pure re-export into logic | mitigate | Preserve the existing zero-`FunctionDef` / zero-`ClassDef` assertions at `tests/test_arena_boundary.py:119-132`; add no callable to the seam |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install occurs. `pyproject.toml` declares `dependencies = []`; `uv.lock` resolves exactly one virtual entry. `02-RESEARCH.md` § "Package Legitimacy Audit" records zero packages and zero `[ASSUMED]`/`[SUS]`/`[SLOP]` verdicts — there is nothing to gate |
</threat_model>

<verification>
1. `uv run python -m unittest tests.test_arena_boundary tests.test_constraint_extractor -v` exits 0.
2. `uv run python -m unittest` exits 0 with no fewer tests than before (384 at phase entry) and adds at least 3.
3. `git show --stat HEAD` for the seam commit lists `arena/evaluator_bridge.py`, `tests/test_arena_boundary.py` and `arena/datasets/__init__.py` together — D-47's one-commit requirement.
4. `grep -rn "_STOPWORDS" .` (excluding `.planning/`) returns nothing.
</verification>

<success_criteria>
- Seam re-exports exactly the eight sorted names; guard asserts that same tuple.
- Boundary scan recurses, excludes `__pycache__`, and exempts only `arena/evaluator_bridge.py` by relative path.
- Two new scanner cases prove the recursion reaches a nested module and that a nested `evaluator_bridge.py` is not exempt.
- `arena/datasets/` exists as a stub package with no callables.
- `STOPWORDS` is public; the private name is gone; the rationale comment names the D-34 consumer.
- Full suite green.
</success_criteria>

## Artifacts this phase produces

**Created by this plan (new symbols / paths):**
- `arena/evaluator_bridge.py` — `__all__` widened to `("behavior_for", "catalog_index", "classify_constraint", "evaluate", "intent_card", "load_jsonl", "materialize_hidden_fields", "searchable_text")`
- `arena/datasets/__init__.py` (new package stub)
- `starter/shopping_agent/constraint_extractor.py` — `STOPWORDS` (public; replaces `_STOPWORDS`)
- `tests/test_arena_boundary.py` — `arena_modules(root)`, `_BRIDGE_RELATIVE_PATH`, `test_bridge_surface_is_exactly_the_declared_names`, `test_scan_reaches_a_nested_module`, `test_a_nested_bridge_named_module_is_not_exempt`

**Created by sibling plans in this phase (referenced, not yet existing):**
`arena/datasets/schema.py`, `arena/datasets/gist.py`, `arena/datasets/divergence.py`,
`arena/datasets/authoring.py`, `arena/datasets/registry.py`, `arena/datasets/generate.py`,
`arena/datasets/assets/*`, `arena/paired_contrast.py`, `data/datasets.json`,
`data/{expanded_dev,expanded_confirm,probe}.v1.jsonl`, `data/responses/*.jsonl`,
`docs/datasets.md`, `experiments/baselines/corpus_baselines.json`,
`experiments/CORPUS_BASELINES.md`, `tests/dataset_fixtures.py`,
`tests/test_datasets_{schema,conformance,gist,divergence,authoring,registry,control_fidelity}.py`,
`tests/test_arena_paired_contrast.py`.

<output>
Create `.planning/phases/02-expanded-dataset-paraphrase-probe/02-01-SUMMARY.md` when done.
</output>
