---
phase: 01-measurement-rig-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - arena/evaluator_bridge.py
  - tests/test_arena_boundary.py
autonomous: true
requirements: [MEAS-15]

must_haves:
  truths:
    - "Exactly one arena module references the evaluator, and a test fails if a second one ever does"
    - "The evaluator seam exposes exactly three names, so `evaluate()` is literally an opaque function to the arena"
    - "`evaluator/local_evaluator.py` is byte-identical to the organizer file, continuously verified rather than promised"
    - "A dynamic-import evasion (`importlib.import_module`, `__import__`) is caught, not just a static import"
  artifacts:
    - path: "arena/evaluator_bridge.py"
      provides: "The sole permitted evaluator seam (D-08)"
      exports: ["evaluate", "catalog_index", "load_jsonl"]
      max_lines: 20
    - path: "tests/test_arena_boundary.py"
      provides: "MEAS-15 as a machine-checked invariant"
      contains: "ast.walk"
  key_links:
    - from: "arena/evaluator_bridge.py"
      to: "evaluator.local_evaluator"
      via: "a single from-import of exactly three names"
      pattern: "from evaluator\\.local_evaluator import catalog_index, evaluate, load_jsonl"
    - from: "tests/test_arena_boundary.py"
      to: "arena/*.py"
      via: "AST walk plus string-constant scan over every module except the bridge"
      pattern: "glob\\(\"\\*\\.py\"\\)"
---

<objective>
Convert MEAS-15 and ROADMAP Success Criterion 5 from a promise into a failing
test. Exactly one module — `arena/evaluator_bridge.py` — may touch `evaluator/`,
its entire re-export surface is three names, and `evaluator/local_evaluator.py`
is pinned by SHA-256.

Purpose: "we never modified the evaluator" is the single strongest Technical
Execution claim this project makes. Right now it is prose. A static-analysis test
makes it a continuously verified fact, and installs the guard in wave 1 so that
every module written by later plans lands underneath it.

Output: the seam module and the boundary test. This plan deliberately touches no
other arena module, so it runs in parallel with plan 01-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-measurement-rig-core/01-CONTEXT.md
@.planning/phases/01-measurement-rig-core/01-RESEARCH.md
@.planning/phases/01-measurement-rig-core/01-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Symbols and paths created by **this plan** (new — not pre-existing drift):

| Kind | Name | File |
|------|------|------|
| module | `arena/evaluator_bridge.py` | the sole evaluator seam |
| re-export | `evaluate` | `arena/evaluator_bridge.py` |
| re-export | `catalog_index` | `arena/evaluator_bridge.py` |
| re-export | `load_jsonl` | `arena/evaluator_bridge.py` |
| constant | `__all__` | `arena/evaluator_bridge.py` |
| test module | `tests/test_arena_boundary.py` | MEAS-15 invariant |
| test class | `ArenaImportBoundaryTest` | `tests/test_arena_boundary.py` |
| test class | `EvaluatorIntegrityTest` | `tests/test_arena_boundary.py` |
| constant | `EVALUATOR_SHA256` | `tests/test_arena_boundary.py` |
| constant | `BRIDGE_EXPORTS` | `tests/test_arena_boundary.py` |

The full phase artifact list is in `01-01-PLAN.md` § "Artifacts this phase produces".

<tasks>

<task type="auto">
  <name>Task 1: Create the sole evaluator seam</name>
  <files>arena/evaluator_bridge.py</files>
  <read_first>
    - `experiments/run_public.py:1-26` (the exact import line at `:13` this module isolates, and the repo's module-header ordering)
    - `evaluator/local_evaluator.py` lines 88-125 and 210-300 — read ONLY to confirm the signatures of `load_jsonl`, `catalog_index` and `evaluate`; do not modify this file under any circumstance
    - `starter/shopping_agent/diagnostics.py:222-226` (the repo's Protocol/seam style)
    - `.planning/phases/01-measurement-rig-core/01-PATTERNS.md` § "`arena/evaluator_bridge.py` (adapter seam, request-response)"
  </read_first>
  <action>
    Create `arena/evaluator_bridge.py`. Its entire body is:

    1. `from __future__ import annotations` as line 1.
    2. A module docstring of two or three lines stating that this is the only
       module in `arena/` permitted to import from `evaluator/` (D-08), that
       `evaluate` is called as an opaque function, and that
       `tests/test_arena_boundary.py` enforces both facts.
    3. The single import `from evaluator.local_evaluator import catalog_index, evaluate, load_jsonl`.
    4. `__all__ = ("catalog_index", "evaluate", "load_jsonl")`.

    Nothing else. No wrapper function, no re-typing, no convenience helper, no
    additional import. Any fourth name pulled through the seam breaks Success
    Criterion 5's "no import of `evaluator.local_evaluator` internals beyond
    calling `evaluate()` as an opaque function", and Task 2's test asserts the
    surface is exactly those three names.

    Do not modify `evaluator/local_evaluator.py`. Its SHA-256 is pinned in Task 2.
  </action>
  <acceptance_criteria>
    - `arena/evaluator_bridge.py` is at most 20 lines including the docstring and blank lines.
    - Its AST contains exactly one `ast.ImportFrom` node, whose `module` is `evaluator.local_evaluator` and whose `names` are exactly `catalog_index`, `evaluate`, `load_jsonl`.
    - Its AST contains zero `ast.Import` nodes other than `from __future__ import annotations`.
    - Its AST contains zero `ast.FunctionDef` and zero `ast.ClassDef` nodes.
    - `__all__ == ("catalog_index", "evaluate", "load_jsonl")`.
    - `uv run python -c "import arena.evaluator_bridge as b; print(b.__all__)"` prints the three names.
    - `git diff --quiet -- evaluator/` exits 0 (the evaluator is untouched).
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "import arena.evaluator_bridge as b; assert b.__all__==('catalog_index','evaluate','load_jsonl'); print('bridge OK')"</automated>
  </verify>
  <done>One module, three re-exported names, zero logic — the arena's only path to the evaluator.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce the boundary with an AST walk, a string-constant scan, and a SHA-256 pin</name>
  <files>tests/test_arena_boundary.py</files>
  <read_first>
    - `arena/evaluator_bridge.py` (as written in Task 1)
    - `tests/test_models.py:1-40` (test module header, `<Subject>Test` class naming, `-> None` on every method, `assertRaises` style)
    - `.planning/phases/01-measurement-rig-core/01-RESEARCH.md` § "Pattern 9: The D-08 import boundary as an AST test" — the seven-evasion-form table and the reference implementation
    - `experiments/analyze_public.py:1-30` (confirm for yourself that this module imports only `subprocess`, `dataclasses`, `enum` — plan 01-04 imports `code_revision` from it, and this test guards that transitive seam)
  </read_first>
  <action>
    Create `tests/test_arena_boundary.py` with two `unittest.TestCase` classes and
    two module constants.

    Module constants:
    - `EVALUATOR_SHA256 = "84ea899707452de249ca62abee77c4b40ab7a3139b5cc798ac30c9f521f91b30"` — the SHA-256 of `evaluator/local_evaluator.py` (13,836 bytes) computed during planning at HEAD `b98ff27`.
    - `BRIDGE_EXPORTS = ("catalog_index", "evaluate", "load_jsonl")`.

    `ArenaImportBoundaryTest`:
    - `test_only_the_bridge_module_references_the_evaluator` — glob `sorted(arena_dir.glob("*.py"))`, skip `evaluator_bridge.py`, `ast.parse` each file, and collect offenders from THREE node kinds, because an import walk alone is insufficient: (a) `ast.Import` where `alias.name.split(".")[0] == "evaluator"`; (b) `ast.ImportFrom` where `(node.module or "").split(".")[0] == "evaluator"` or `node.level` is non-zero and `"evaluator"` appears in the dotted module parts; (c) `ast.Constant` holding a `str` whose first dotted component is `"evaluator"` — this is what closes the `importlib.import_module("evaluator.local_evaluator")` and `__import__("evaluator...")` holes that a pure import walk misses. Assert the offenders mapping equals `{}` and make the failure message name the offending file and the offending reference.
    - `test_bridge_surface_is_exactly_three_names` — parse `arena/evaluator_bridge.py` by AST (do not import it, so the assertion holds even if the evaluator is unimportable): assert exactly one non-`__future__` `ast.ImportFrom`, its module is `evaluator.local_evaluator`, its alias names sorted equal `list(BRIDGE_EXPORTS)`, and the module defines no function and no class.
    - `test_arena_package_has_modules_to_scan` — assert the glob found at least one non-bridge `arena/*.py` file, so the boundary test can never pass vacuously on an empty package.
    - `test_analyze_public_does_not_reach_the_evaluator` — run the same three-kind scan over `experiments/analyze_public.py` and assert zero references. Plan 01-04 imports `code_revision` from that module; without this guard a future change there would breach D-08 transitively, invisible to a scan restricted to `arena/`.

    `EvaluatorIntegrityTest`:
    - `test_evaluator_is_byte_unmodified` — `hashlib.sha256(Path("evaluator/local_evaluator.py").read_bytes()).hexdigest()` equals `EVALUATOR_SHA256`. Use `read_bytes()`, never `read_text()`, so a line-ending change is caught. Failure message must say that the evaluator is immutable by hard invariant and that any legitimate organizer update requires re-pinning this constant deliberately.

    Locate the repository root as `Path(__file__).resolve().parent.parent` — do not
    depend on the process working directory.

    Style: line 1 `from __future__ import annotations`; stdlib imports
    (`ast`, `hashlib`, `unittest`, `pathlib.Path`) alphabetical; every test method
    annotated `-> None`; no third-party imports.
  </action>
  <acceptance_criteria>
    - `uv run python -m unittest -v tests.test_arena_boundary` exits 0 with 5 tests run.
    - The suite passes with no catalog and no 580 MB artifact present (the test performs no `Agent` construction and no SQLite access).
    - Temporarily adding a line `import evaluator.local_evaluator` to any `arena/*.py` other than the bridge makes `test_only_the_bridge_module_references_the_evaluator` FAIL; revert afterwards.
    - Temporarily adding `importlib.import_module("evaluator.local_evaluator")` to any `arena/*.py` other than the bridge makes the same test FAIL; revert afterwards.
    - Temporarily appending a single newline to a scratch copy of `evaluator/local_evaluator.py` changes its SHA-256 away from `84ea899707452de249ca62abee77c4b40ab7a3139b5cc798ac30c9f521f91b30` (demonstrate on a copy — never edit the real file).
    - `EVALUATOR_SHA256 == "84ea899707452de249ca62abee77c4b40ab7a3139b5cc798ac30c9f521f91b30"`.
    - `uv run python -W error::ResourceWarning -m unittest -v` passes (167 existing + 5 new).
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m unittest -v tests.test_arena_boundary</automated>
  </verify>
  <done>MEAS-15 is a test that fails on a real breach, including dynamic-import evasion and a transitive breach via `experiments/analyze_public.py`, and the evaluator's bytes are pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `arena/` → `evaluator/` | The competition's scoring authority. Any arena code that reads evaluator internals, or any edit to the evaluator, invalidates every number this project reports |
| `arena/` → `experiments/` | A transitive import path that could reach the evaluator without appearing in an `arena/`-only scan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Tampering | `evaluator/local_evaluator.py` | mitigate | `EvaluatorIntegrityTest.test_evaluator_is_byte_unmodified` pins SHA-256 `84ea8997...f91b30` over `read_bytes()`; `git diff --quiet -- evaluator/` in Task 1 acceptance |
| T-01-02b | Tampering | arena modules reaching evaluator internals | mitigate | AST `Import`/`ImportFrom` walk plus `ast.Constant` string scan over every `arena/*.py` except the bridge, verified against static, aliased, relative, function-local, `importlib` and `__import__` forms |
| T-01-02c | Tampering | transitive breach via `experiments/analyze_public.py` | mitigate | `test_analyze_public_does_not_reach_the_evaluator` runs the same three-kind scan on that module |
| T-01-10 | Denial of Service | vacuous pass on an empty `arena/` | mitigate | `test_arena_package_has_modules_to_scan` asserts the glob is non-empty so the invariant cannot pass by finding nothing |
| T-01-09 | Spoofing | SHA-256 pin treated as tamper-evidence against an attacker | accept | Single local user, no adversary with write access; the pin detects accidental or careless modification, which is the realistic threat |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed in this phase; `dependencies = []` and `uv.lock` holds only the virtual root |
</threat_model>

<verification>
- `uv run python -m unittest -v tests.test_arena_boundary` — 5 tests, all green.
- `uv run python -W error::ResourceWarning -m unittest -v` — full suite green.
- `git diff --quiet -- evaluator/` exits 0.
</verification>

<success_criteria>
- `arena/evaluator_bridge.py` is the only arena module that names `evaluator`, proven by a test that also catches dynamic imports.
- The bridge re-exports exactly `evaluate`, `catalog_index`, `load_jsonl`.
- `evaluator/local_evaluator.py`'s SHA-256 is asserted on every test run.
</success_criteria>

<output>
Create `.planning/phases/01-measurement-rig-core/01-02-SUMMARY.md` when done.
</output>
