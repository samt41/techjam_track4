---
phase: 01-measurement-rig-core
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - arena/adjudication.py
  - tests/test_arena_adjudication.py
autonomous: true
gap_closure: true
requirements: [MEAS-07]

must_haves:
  truths:
    - "A candidate that regresses HR@10 while improving MTTC is reported as NOT a win, and `hr10_exchange_rate` appears in its `failed_criteria`"
    - "The practical-significance floor is tested against the winner's-curse-corrected delta on EVERY adjudication row, including zero-variance ones"
    - "No adjudication row emits a fabricated `permutation_p`, `holm_p`, `minimum_detectable_difference` or `clears_practical_floor` — every one is measured on the general path"
    - "A uniform per-session improvement of +0.15 TechnicalScore is not reported as `no difference`"
    - "Two candidates identical on every session are still reported `no difference`, never a win — now because the measured statistics say so"
    - "Adjudicating the same inputs twice still produces byte-identical output"
  artifacts:
    - path: "arena/adjudication.py"
      provides: "The repaired D-23 exchange-rate criterion, a delta-conditioned degeneracy definition, and a fabrication-free row"
      exports: ["PRACTICAL_FLOOR", "EXCHANGE_RATE_PER_MTTC", "CRITERION_ORDER", "Verdict", "CandidateArm", "AdjudicationRow", "classify_verdict", "adjudicate"]
      contains: "abs(mttc_delta)"
    - path: "tests/test_arena_adjudication.py"
      provides: "Regression fixtures for both verification blockers, plus explicit regression guards for the two truths that currently pass"
  key_links:
    - from: "arena/adjudication.py"
      to: "arena/statistics.py"
      via: "paired_permutation is called unconditionally, once per candidate"
      pattern: "paired_permutation"
    - from: "arena/adjudication.py"
      to: "arena/statistics.py"
      via: "degeneracy is conditioned on the bootstrap delta as well as its standard error"
      pattern: "ZERO_VARIANCE_TOLERANCE"
---

<objective>
Repair the two reproducible false verdicts that `01-VERIFICATION.md` records against
`arena/adjudication.py`, and close the two adjudication-side warnings that live in the
same twenty lines of source.

Purpose: the phase goal's operative adjective is *statistically honest*. Everything
upstream of `adjudicate()` verified cleanly; the single function that converts
measurement into judgement produces a `win` on a candidate that regresses both headline
retrieval metrics, and reports a `+0.15` TechnicalScore gain as `no difference` on values
it asserted rather than measured. Phase 3's Success Criterion 5 depends directly on the
first rule, and its entire CONV workstream is designed to move MTTC in exactly the
direction that switches that rule off.

Why one plan rather than two: both blockers, the `failures`-mapping rename (WR-13) and
the Holm-budget question (WR-05) all sit inside a single ~40-line region of
`arena/adjudication.py` — the permutation loop, the row loop, and the criteria mapping.
Splitting them across plans would serialize four edits to one code path across four
waves and force three merge conflicts on the same hunks, for no separability benefit:
none of the four can be reasoned about without the other three in view. They are one
coherent edit.

Output: an adjudication module in which no emitted field is a hard-coded constant, and a
test suite that fails if either false verdict ever returns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-measurement-rig-core/01-CONTEXT.md
@.planning/phases/01-measurement-rig-core/01-VERIFICATION.md
@.planning/phases/01-measurement-rig-core/01-REVIEW.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Symbols created or renamed by **this plan** (new — not pre-existing drift):

| Kind | Name | File |
|------|------|------|
| dataclass field | `AdjudicationRow.is_degenerate` | `arena/adjudication.py` |
| payload key | `is_degenerate` (on every `AdjudicationRow.as_record()` mapping) | `arena/adjudication.py` |
| local rename | `failures` → `passed` (criteria mapping inside `adjudicate`) | `arena/adjudication.py` |
| test class | `ExchangeRateSignTest`, `ZeroVarianceTest` | `tests/test_arena_adjudication.py` |
| test fixture | `_MTTC_TRADE_BASELINE`, `_MTTC_TRADE_DOUBLE_REGRESSION`, `_MTTC_TRADE_UNDERPAID_GAIN`, `_MTTC_TRADE_PAID_GAIN`, `_UNIFORM_BASELINE`, `_UNIFORM_PROMOTED` | `tests/test_arena_adjudication.py` |
| test method | `test_double_regression_with_an_mttc_gain_is_not_a_win`, `test_an_mrr_gain_below_the_magnitude_bar_does_not_buy_an_hr10_regression`, `test_an_mrr_gain_above_the_magnitude_bar_does_buy_an_hr10_regression`, `test_uniform_improvement_is_not_reported_as_no_difference`, `test_identical_arms_remain_no_difference_on_measured_statistics`, `test_degenerate_arms_stay_in_the_holm_family_and_correction_k`, `test_no_row_field_is_a_fabricated_constant` | `tests/test_arena_adjudication.py` |

Nothing is deleted from the public surface. `PRACTICAL_FLOOR`, `SIGNIFICANCE_ALPHA`,
`EXCHANGE_RATE_PER_MTTC`, `CRITERION_ORDER`, `Verdict`, `CandidateArm`,
`classify_verdict` and `adjudicate` keep their existing names, values and signatures.

<tasks>

<task type="auto">
  <name>Task 1: Make the D-23 exchange-rate criterion fire when MTTC improves</name>
  <files>arena/adjudication.py</files>
  <read_first>
    - `arena/adjudication.py` — the whole file, and specifically lines 20-41 (the `EXCHANGE_RATE_PER_MTTC` docstring comment and `CRITERION_ORDER`), lines 287-307 (the floor, the exchange-rate expression, and the `failures` mapping)
    - `.planning/phases/01-measurement-rig-core/01-VERIFICATION.md` — the YAML frontmatter `gaps:` block, FIRST entry (truth 6), including its `missing:` list and the `## CR-02 reproducer output (executed)` section with the measured blast-radius table
    - `.planning/phases/01-measurement-rig-core/01-REVIEW.md` § "CR-02" and § "WR-13: The `failures` mapping in `adjudicate` holds passes, not failures"
    - `.planning/phases/01-measurement-rig-core/01-CONTEXT.md` decision D-23 and decision D-16
    - `tests/test_arena_adjudication.py` lines 84-95 — the existing `_TRADE_BASELINE` / `_TRADE_UNDERPAID` / `_TRADE_PAID` fixtures, so the change can be checked against what already exists
  </read_first>
  <action>
    Two edits to `arena/adjudication.py`, both inside the non-degenerate branch of the
    row loop. No new constant, no new function, no signature change.

    **Edit 1 — the exchange-rate expression (adjudication.py:295-297).** Replace the
    current two-clause form with a three-clause one whose target state is exactly:
    `exchange_rate_ok` is `True` when `hit_rate_delta >= 0.0`, and otherwise `True` only
    when both `mrr_delta > 0.0` and `mrr_delta > EXCHANGE_RATE_PER_MTTC * abs(mttc_delta)`.
    `EXCHANGE_RATE_PER_MTTC` stays exactly `0.0667`.

    Both new clauses are load-bearing and neither substitutes for the other. `abs()`
    fixes the sign inversion: `mttc_delta = candidate_mttc - baseline_mttc`, so an MTTC
    *improvement* is negative and the un-absoluted right-hand side goes negative,
    turning the comparison into `mrr_delta > <negative number>` — satisfied by a
    NEGATIVE `mrr_delta`. `mrr_delta > 0.0` fixes the semantics: the constant's own
    comment promises the regression is forgiven "only when the MRR **gain** exceeds"
    the bar, and there is no gain in a negative delta.

    Extend the comment block at lines 31-35 (above `EXCHANGE_RATE_PER_MTTC`) and the
    inline comment at lines 293-294 to record, as *why* not *what*:
    - The measured failure this replaces: a candidate regressing HR@10 by `0.030` and
      MRR by `0.010` with an MTTC improvement of `4.11` turns was adjudicated
      `verdict = win` with `failed_criteria = ()` against the checked-in code. A 4-turn
      MTTC improvement licensed an MRR regression of up to `0.274` — larger than this
      project's entire MRR headroom.
    - `mttc_delta < 0` is the DESIGNED direction of improvement for the whole Phase 3
      CONV workstream, so this was the main path, not an edge case.
    - CONV-03 and CLAUDE.md both state the principle the criterion enforces: a recall
      regression cannot be bought with speed.

    Explicitly out of scope, both offered to the operator and explicitly DECLINED: a
    hard HR@10 regression floor, and scaling the forgiveness threshold with the size of
    the HR@10 regression. An HR@10 regression of any size remains forgivable when the
    MRR gain clears the magnitude-scaled bar. Do not add either. Do not change
    `EXCHANGE_RATE_PER_MTTC`.

    **Edit 2 — WR-13, the inverted mapping name (adjudication.py:298-307).** Rename the
    local dict `failures` to `passed`. Every value it holds is `True` when the criterion
    PASSED, so the current name inverts its own meaning inside the most safety-critical
    function in the rig. The comprehension keeps its `not` and becomes
    `tuple(name for name in CRITERION_ORDER if not passed[name])`. Add one comment line
    stating that the mapping holds PASSES, so a future `if passed[name]` reads in the
    obvious sense and cannot silently invert every verdict in the report. This is a
    zero-behaviour-change rename; do not reorder the keys and do not change
    `CRITERION_ORDER`.

    Leave `classify_verdict`, `PRACTICAL_FLOOR`, `SIGNIFICANCE_ALPHA` and the
    `clears_practical_floor = corrected_delta >= PRACTICAL_FLOOR` line untouched.
  </action>
  <acceptance_criteria>
    - `grep -v '^\s*#' arena/adjudication.py | grep -c 'abs(mttc_delta)'` returns `1` — comments are stripped first, because this same task instructs the executor to quote the expression in an explanatory comment, and an unfiltered count would make a correct implementation fail its own gate. Keep the count EXACT rather than `>= 1`: it is what forbids a second, contradictory exchange-rate expression from creeping in.
    - `grep -v '^\s*#' arena/adjudication.py | grep -c 'mrr_delta > 0.0'` returns `1` — same comment stripping, for the same reason, and the same exact count. Do not relax either of these two gates to `>= 1`.
    - `grep -v '^\s*#' arena/adjudication.py | grep -c 'EXCHANGE_RATE_PER_MTTC \* mttc_delta'` returns `0` — the vacuous form is gone from executable source.
    - `grep -v '^\s*#' arena/adjudication.py | grep -c 'failures'` returns `0`; `grep -v '^\s*#' arena/adjudication.py | grep -c 'passed\['` returns `1`.
    - `python -c "import arena.adjudication as a; print(a.EXCHANGE_RATE_PER_MTTC, a.PRACTICAL_FLOOR, a.SIGNIFICANCE_ALPHA, a.CRITERION_ORDER)"` prints `0.0667 0.01 0.05 ('holm_significance', 'practical_floor', 'hr10_exchange_rate')`.
    - `grep -v '^\s*#' arena/adjudication.py | grep -cE 'hit_rate_delta (<|<=|>=) -?0\.0[0-9]'` returns `0` — no HR@10 regression-size floor or size-scaled forgiveness was added.
    - `uv run python -m unittest -v tests.test_arena_adjudication` exits 0. In particular `test_hr10_regression_without_exchange_rate_clearance_is_not_a_win` and `test_hr10_regression_with_exchange_rate_clearance_passes_that_criterion` still pass unchanged: both existing fixtures have `mttc_delta = +0.18`, so `abs()` is a no-op for them, and both have a positive `mrr_delta` (`+0.010` and `+0.030`), so the new gain clause holds for both.
    - `git diff --name-only` lists `arena/adjudication.py` and nothing else.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m unittest -v tests.test_arena_adjudication</automated>
  </verify>
  <done>The one criterion whose job is to stop an HR@10 regression from shipping now fires in the direction it was written for, and the mapping that decides every verdict is named for what it holds.</done>
</task>

<task type="auto">
  <name>Task 2: Stop asserting adjudication values and start measuring them</name>
  <files>arena/adjudication.py</files>
  <read_first>
    - `arena/adjudication.py` as amended by Task 1 — specifically lines 202-216 (the degeneracy definition and the permutation short-circuit), lines 230-246 (the Holm call and `correction_k`), lines 258-320 (the row loop and its `if is_degenerate:` block), and lines 61-120 (`AdjudicationRow` and `as_record`)
    - `arena/statistics.py` — `ZERO_VARIANCE_TOLERANCE`, `paired_permutation` (note the Phipson-Smyth `(count + 1) / (resamples + 1)`), `holm_bonferroni`, `minimum_detectable_difference`, `winners_curse_correction`
    - `.planning/phases/01-measurement-rig-core/01-VERIFICATION.md` — the YAML frontmatter `gaps:` block, SECOND entry (truth 7), and the `### CR-01 reproducer output (executed)` block showing both what the guard emits and what the general path would have emitted on the identical fixture
    - `.planning/phases/01-measurement-rig-core/01-REVIEW.md` § "CR-01" and § "WR-05: Degenerate arms consume Holm family budget and `correction_k` on an asserted, never-measured p-value"
    - `tests/test_arena_adjudication.py` lines 78-83 and 110-125 — the `_FLOOR_*` fixture and `test_floor_is_applied_to_the_corrected_delta`, which adjudicates a real arm alongside a deliberately identical `"null"` arm and depends on `correction_k == 2`
    - `starter/shopping_agent/diagnostics.py:29-38` — the `as_record()` convention a new field must follow
  </read_first>
  <action>
    Four edits to `arena/adjudication.py`. The governing rule: **no emitted field on any
    adjudication row may be a fabricated constant.**

    **Edit 1 — narrow the degeneracy definition (adjudication.py:207-209).** Condition
    it on the delta as well as the standard error, so the tuple comprehension tests
    `result.standard_error <= ZERO_VARIANCE_TOLERANCE and abs(result.delta) <= ZERO_VARIANCE_TOLERANCE`
    for each bootstrap result. Bootstrap SE is exactly zero for any exactly-uniform
    per-session improvement, not only for identical arms, so SE alone does not mean what
    the name `degenerate` claims. Do not change `ZERO_VARIANCE_TOLERANCE` itself in
    `arena/statistics.py`.

    **Edit 2 — measure the permutation for every candidate (adjudication.py:213-216).**
    Delete the `if degenerate[index]:` guard that appends a literal `1.0` and `continue`s.
    `paired_permutation` now runs unconditionally, once per candidate, with the same
    `pair_seed(..., "permutation")` seeding it already uses. It is cheap and it returns an
    honest Phipson-Smyth answer for identical arms — every sign-flip assignment of two
    identical arms produces a statistic tied with the observed one, so the measured
    p-value is exactly `(resamples + 1) / (resamples + 1) = 1.0`, which is the same
    number the guard asserted but now arrived at by measurement.

    **Edit 3 — delete the value-fabricating branch (adjudication.py:264-278).** Remove
    the entire `if is_degenerate:` block and collapse its `else:` so that `holm_p`,
    `detectable_difference`, `corrected_delta`, `clears_practical_floor`,
    `exchange_rate_ok`, `passed` and `failed_criteria` are computed exactly once,
    unconditionally, on what was the general path. After this edit no assignment in the
    module sets `holm_p`, `permutation_p`, `minimum_detectable_difference` or
    `clears_practical_floor` to a literal.

    Verify by reasoning at the branch and record it in the replacement comment: for two
    identical arms the general path now yields `delta 0.0`, `standard_error 0.0`,
    measured `permutation_p 1.0`, `holm_p 1.0`, `mdd = 2.801585218112968 * 0.0 = 0.0`,
    `corrected_delta = 0.0 - 0.0 * expected_max_of_k(k) = 0.0`,
    `clears_practical_floor = (0.0 >= 0.01) = False`, `exchange_rate_ok = True` because
    `hit_rate_delta == 0.0`, and therefore
    `failed_criteria == ("holm_significance", "practical_floor")`, which `classify_verdict`
    turns into `Verdict.NO_DIFFERENCE` at clause 4 — identical to what the branch
    asserted, now measured. Truth 8 survives unchanged.

    Replace the Pitfall-5 comment at lines 202-206 with one that states what is now
    true: the guard no longer changes any emitted value; the `abs(delta) >= mdd` reading
    `0 >= 0` as True that Pitfall 5 warns about is handled by `classify_verdict`
    clause 3 falling through to `NO_DIFFERENCE`, which is the correct answer for two
    identical arms and is reached through the general rule rather than a special case.

    **Edit 4 — surface degeneracy instead of acting on it.** Add a field
    `is_degenerate: bool` to `AdjudicationRow`, positioned immediately after
    `standard_error`, and add the matching `"is_degenerate"` key to `as_record()` in the
    same position. Populate it from the narrowed tuple. Comment that this field is
    DESCRIPTIVE ONLY: it is derived from two measured quantities, it is never used to
    fabricate or override any other field, and it exists so that a reader of the report
    can see which arms agreed with the baseline on every session. This is what keeps
    Edit 1 load-bearing after Edit 3 removes the branch it used to gate.

    **The WR-05 Holm-family answer, to be recorded as comments beside
    `holm_bonferroni(...)` at line 233 and beside `correction_k` at lines 244-246.** The
    deliberate decision is: **a genuinely degenerate arm REMAINS in the Holm family and
    REMAINS counted in `correction_k`.** Write down all four reasons, because Phase 3
    will be adjudicated by this rule and must be able to re-derive why:
    1. The family size and `k` are properties of the experimental DESIGN — how many arms
       were submitted for comparison. Shrinking either after seeing which arms turned out
       degenerate is a data-dependent family definition, which is anti-conservative and
       is precisely the selection sin the winner's-curse correction exists to price.
    2. `arena/run_arena.py`'s `--include` already provides an A-PRIORI mechanism for
       retained records that belong in the report without joining the family, so no
       post-hoc exclusion is needed; an operator who knows an arm is not a hypothesis
       says so before the run, not after.
    3. What WR-05 actually objected to was a SYNTHETIC p-value entering the family. Edit
       2 removes it: the `1.0` in the family is now measured.
    4. Concretely: `tests/test_arena_adjudication.py`'s `_FLOOR_*` fixture adjudicates a
       real arm alongside a deliberately identical `"null"` arm and depends on
       `correction_k == 2`. Post-hoc exclusion would set `k = 1`,
       `expected_max_of_k(1) == 0.0` would remove the correction entirely, and the D-20
       floor-ordering tripwire would then pass on the raw delta — silently disabling the
       single ordering guarantee this phase exists to provide.

    Do not touch `classify_verdict`, `CRITERION_ORDER`, `PRACTICAL_FLOOR`, or the
    `clears_practical_floor = corrected_delta >= PRACTICAL_FLOOR` expression.
  </action>
  <acceptance_criteria>
    - `grep -c 'abs(result.delta) <= ZERO_VARIANCE_TOLERANCE' arena/adjudication.py` returns `1`.
    - `grep -v '^\s*#' arena/adjudication.py | grep -c 'if degenerate\[index\]'` returns `0`, and `grep -v '^\s*#' arena/adjudication.py | grep -c 'if is_degenerate'` returns `0`.
    - `grep -v '^\s*#' arena/adjudication.py | grep -cE '(holm_p|permutation_p|detectable_difference|clears_practical_floor) = (1\.0|0\.0|False|True)$'` returns `0` — no emitted field is assigned a literal.
    - `grep -c 'paired_permutation(' arena/adjudication.py` returns `1`, and it is reached on every loop iteration.
    - `python -c "import dataclasses, arena.adjudication as a; print([f.name for f in dataclasses.fields(a.AdjudicationRow)])"` includes `is_degenerate` immediately after `standard_error`, and the field list has length `24`.
    - Behavioural, uniform promotion: adjudicating `arm('base', sessions_from_ranks((2,)*200))` against `arm('promoted', sessions_from_ranks((1,)*200))` at `resamples=500` yields `is_degenerate is False`, `permutation_p == 1.0 / 501`, `corrected_delta` within `1e-9` of `0.15`, `clears_practical_floor is True`, `failed_criteria == ()` and `verdict is Verdict.WIN`.
    - Behavioural, identical arms: adjudicating two arms built from the SAME session tuple yields `is_degenerate is True`, `permutation_p == 1.0` (measured — the code path contains no literal that could produce it), `minimum_detectable_difference == 0.0`, `corrected_delta == 0.0`, `clears_practical_floor is False`, `failed_criteria == ("holm_significance", "practical_floor")`, and `verdict is Verdict.NO_DIFFERENCE`.
    - `test_floor_is_applied_to_the_corrected_delta` still passes and its rows still report `correction_k == 2`.
    - `uv run python -m unittest -v tests.test_arena_adjudication` exits 0 in under 20 seconds (the degenerate arm now runs a real permutation, so a small increase over the previous 0.66 s is expected and acceptable).
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m unittest -v tests.test_arena_adjudication</automated>
  </verify>
  <done>Every number on every adjudication row is measured, the row can no longer contradict itself, and the family-size question has a written answer with its reasoning attached.</done>
</task>

<task type="auto">
  <name>Task 3: Pin both false verdicts and guard the two truths that currently pass</name>
  <files>tests/test_arena_adjudication.py</files>
  <read_first>
    - `arena/adjudication.py` as amended by Tasks 1 and 2
    - `tests/test_arena_adjudication.py` — the whole file, especially lines 30-46 (`FAST_RESAMPLES` / `STABLE_RESAMPLES` and their budget comment), lines 49-70 (the `arm` factory), lines 72-107 (every existing module fixture), and `DegenerateTest` / `ReproducibilityTest`
    - `tests/arena_fixtures.py` — `session`, `sessions_from_ranks` (note its `turn` keyword, applied uniformly, and its zero-padded `s{index:03d}` sample ids), `load_anchor_sessions`, `promote_hits_to_rank_one`
    - `.planning/phases/01-measurement-rig-core/01-VERIFICATION.md` — the `### CR-01 reproducer output (executed)` and `### CR-02 reproducer output (executed)` blocks; these are the two executed reproducers this task inverts into tests
    - `.planning/phases/01-measurement-rig-core/01-VALIDATION.md` § "What 'validated' means for a statistical engine"
  </read_first>
  <action>
    Extend `tests/test_arena_adjudication.py`. No `Agent`, no SQLite, no catalog. Reuse
    the existing `arm(...)` factory and the existing `FAST_RESAMPLES` / `STABLE_RESAMPLES`
    constants; add no third resample constant.

    **New module-level fixtures.** All are paired: every candidate must carry the same
    `sample_id` sequence as its baseline, so build each candidate with
    `dataclasses.replace` over its baseline tuple rather than by concatenating fresh
    `sessions_from_ranks` calls, which would restart the `s000` numbering and trip
    `_require_paired`.

    - `_MTTC_TRADE_BASELINE` — `sessions_from_ranks((3,) * 100, turn=8)`. HR@10 `1.00`,
      MRR `0.333333`, MTTC `8.00`.
    - `_MTTC_TRADE_DOUBLE_REGRESSION` — the verifier's executed reproducer. From
      `_MTTC_TRADE_BASELINE`: turn the first 3 sessions into misses (`hit=False`,
      `best_rank=None`, `first_hit_turn=None`, `reciprocal_rank=0.0`), and give 60 of the
      remaining 97 `first_hit_turn=1` with `best_rank` left at 3. Measured result:
      `hit_rate_delta = -0.03`, `mrr_delta = -0.01`, `mttc_delta = -4.11`.
    - `_MTTC_TRADE_UNDERPAID_GAIN` — an HR@10 regression, an MTTC improvement, and a
      SMALL but genuinely POSITIVE `mrr_delta` that sits BELOW
      `EXCHANGE_RATE_PER_MTTC * abs(mttc_delta)`. This is the fixture that proves `abs()`
      is load-bearing: under the pre-fix rule its bar was negative and it passed.
      Calibrate it yourself from a `turn=3` baseline (a smaller MTTC movement makes the
      bar reachable); the required properties are asserted in the test, so the exact rank
      tuple is yours to choose.
    - `_MTTC_TRADE_PAID_GAIN` — the mirror: an HR@10 regression and an MTTC improvement
      with an `mrr_delta` ABOVE that same bar, so the criterion still forgives a genuinely
      paid-for trade and has not simply been switched off in the other direction.
    - `_UNIFORM_BASELINE` — `sessions_from_ranks((2,) * 200)`.
    - `_UNIFORM_PROMOTED` — `sessions_from_ranks((1,) * 200)`. Uniform rank-2 to rank-1
      promotion: HR@10 and MTTC are invariant, so `delta` is exactly `0.30 * 0.5 = 0.15`
      up to the 6-dp rounding of TechnicalScore, and the bootstrap SE is exactly `0.0`
      because the delta does not depend on which sessions are resampled.

    Comment each fixture with the quantity it is calibrated to hold, matching the style
    of the existing `_TRADE_*` and `_FLOOR_*` comments.

    **New class `ExchangeRateSignTest`**, all at `FAST_RESAMPLES`:
    - `test_double_regression_with_an_mttc_gain_is_not_a_win` — the CR-02 reproducer,
      inverted. Assert the measured deltas first as a non-vacuity guard
      (`hit_rate_delta` ~ `-0.03`, `mrr_delta` ~ `-0.01`, `mttc_delta` ~ `-4.11`, each to
      `places=6`), then `exchange_rate_ok is False`, `"hr10_exchange_rate" in failed_criteria`,
      and `verdict is not Verdict.WIN`. Record on the method that this exact input was
      adjudicated `win` with `failed_criteria == ()` against the pre-fix code.
    - `test_an_mrr_gain_below_the_magnitude_bar_does_not_buy_an_hr10_regression` — assert
      `hit_rate_delta < 0.0`, `mttc_delta < 0.0`, `mrr_delta > 0.0` and
      `mrr_delta < EXCHANGE_RATE_PER_MTTC * abs(mttc_delta)` as non-vacuity guards, then
      `exchange_rate_ok is False` and `"hr10_exchange_rate" in failed_criteria`.
    - `test_an_mrr_gain_above_the_magnitude_bar_does_buy_an_hr10_regression` — the same
      four guards with the last inequality reversed, then `exchange_rate_ok is True` and
      `"hr10_exchange_rate" not in failed_criteria`.

    **New class `ZeroVarianceTest`**, at `STABLE_RESAMPLES` unless noted:
    - `test_uniform_improvement_is_not_reported_as_no_difference` — the CR-01 reproducer,
      inverted. Adjudicate `_UNIFORM_PROMOTED` against `_UNIFORM_BASELINE` as a SINGLE
      candidate. Assert `assertAlmostEqual(row.delta, 0.15, places=9)`,
      `standard_error == 0.0`, `is_degenerate is False`,
      `permutation_p == 1.0 / (STABLE_RESAMPLES + 1)` (every sign flip that moves any
      session changes the statistic, so the measured p lands exactly on the
      Phipson-Smyth floor), `clears_practical_floor is True`, `failed_criteria == ()`,
      `verdict is Verdict.WIN`, and explicitly `verdict is not Verdict.NO_DIFFERENCE`.
      Record on the method that the pre-fix code returned `no difference` on a `+0.15`
      delta — fifteen times the ship floor — with `permutation_p` asserted rather than
      measured.
    - `test_identical_arms_remain_no_difference_on_measured_statistics` — the truth-8
      REGRESSION GUARD, not a re-derivation. Two arms built from the same session tuple:
      assert `is_degenerate is True`, `delta == 0.0`, `standard_error == 0.0`,
      `permutation_p == 1.0`, `holm_p == 1.0`, `minimum_detectable_difference == 0.0`,
      `corrected_delta == 0.0`, `clears_practical_floor is False`,
      `exchange_rate_ok is True`,
      `failed_criteria == ("holm_significance", "practical_floor")`,
      `verdict is Verdict.NO_DIFFERENCE` and `verdict is not Verdict.WIN`. Note on the
      method that every one of these values is now produced by the general path.
    - `test_degenerate_arms_stay_in_the_holm_family_and_correction_k` — pins the WR-05
      decision so a future edit cannot silently change the family. Adjudicate one real
      candidate and one identical-to-baseline candidate together, then assert every row
      reports `candidate_count == 2` and `correction_k == 2`, that
      `expected_max_of_k` on each row equals `arena.statistics.expected_max_of_k(2)`, and
      that each row's `holm_p` equals the value `holm_bonferroni` returns for the tuple of
      the rows' `permutation_p` values in the same order.
    - `test_no_row_field_is_a_fabricated_constant` — over a mixed adjudication that
      includes a degenerate arm, assert for every row that
      `minimum_detectable_difference == arena.statistics.minimum_detectable_difference(row.standard_error)`
      exactly, that `corrected_delta` equals
      `row.delta - row.standard_error * row.expected_max_of_k` to `places=12`, and that
      `clears_practical_floor == (row.corrected_delta >= PRACTICAL_FLOOR)`. These three
      identities are re-derivations of the row from its own printed columns; before this
      plan a degenerate row failed the third one.

    Do NOT assert exact `ci_lower` / `ci_upper` values anywhere in the new tests. Plan
    01-11 changes the percentile index arithmetic in the same wave; asserting a CI bound
    here would create a false conflict. Assertions of the form "the CI contains the
    delta" are fine.

    Leave `ReproducibilityTest` untouched: it is the truth-10 regression guard and must
    keep passing byte-identically.
  </action>
  <acceptance_criteria>
    - `uv run python -m unittest -v tests.test_arena_adjudication` exits 0 with at least `32` test methods and completes in under 25 seconds.
    - `ExchangeRateSignTest` has exactly 3 test methods and `ZeroVarianceTest` has exactly 4.
    - `test_double_regression_with_an_mttc_gain_is_not_a_win` FAILS when the `abs(` is removed from the exchange-rate expression in `arena/adjudication.py`; revert the mutation afterwards and confirm `git diff --stat` is clean for `arena/adjudication.py`.
    - `test_uniform_improvement_is_not_reported_as_no_difference` FAILS when the `and abs(result.delta) <= ZERO_VARIANCE_TOLERANCE` clause is removed from the degeneracy tuple; revert the mutation afterwards and confirm `git diff --stat` is clean for `arena/adjudication.py`.
    - `grep -c 'ci_lower\|ci_upper' tests/test_arena_adjudication.py` does not increase relative to the file at the start of this task (record both counts in the SUMMARY).
    - `grep -c 'Agent' tests/test_arena_adjudication.py` returns `0`.
    - `ReproducibilityTest` still passes, including `test_reproducible_across_processes` with `PYTHONHASHSEED=0` and `PYTHONHASHSEED=1` producing byte-identical stdout.
    - `uv run python -W error::ResourceWarning -m unittest discover -s tests` passes the full suite with no new failures.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -W error::ResourceWarning -m unittest discover -s tests</automated>
  </verify>
  <done>Both executed reproducers from `01-VERIFICATION.md` are now tests that fail against the pre-fix code, and the two truths that already passed are held down by explicit regression guards rather than by luck.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| statistics → shipped decision | This module converts numbers into "ship this candidate". A logic error here is invisible downstream and directly selects the competition submission |
| emitted row → reader's belief | An `AdjudicationRow` is evidence a judge may read. A hard-coded field in a column presented as measured is a repudiation surface regardless of whether the value is arithmetically right |

Not applicable, with reasons: no network endpoint, no credential, no untrusted input
(the module is a pure transform over frozen dataclasses supplied by an in-process
caller), no filesystem write, no deserialization, no privilege boundary, no
multi-tenant surface. ASVS L1 categories for authentication, session management,
access control, and cryptographic storage have no corresponding component here.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-22 | Tampering | `exchange_rate_ok` sign inversion in `adjudicate` | mitigate | Require `mrr_delta > 0.0` and compare against `EXCHANGE_RATE_PER_MTTC * abs(mttc_delta)`; `test_double_regression_with_an_mttc_gain_is_not_a_win` is mutation-verified against removal of the `abs(` |
| T-01-23 | Repudiation | adjudication row emitting a fabricated `permutation_p` / `mdd` / `clears_practical_floor` | mitigate | The value-fabricating branch is deleted; `test_no_row_field_is_a_fabricated_constant` re-derives three identities from the row's own printed columns |
| T-01-24 | Tampering | data-dependent Holm family or `correction_k` | mitigate | Degenerate arms stay in the family and in `k` by written decision; `test_degenerate_arms_stay_in_the_holm_family_and_correction_k` pins it, and `--include` remains the a-priori exclusion mechanism |
| T-01-25 | Repudiation | a criteria mapping whose name inverts its values | mitigate | `failures` renamed to `passed`; grep acceptance criterion asserts the old identifier is absent from executable source |
| T-01-14 | Tampering | practical floor applied to the raw delta | mitigate | Unchanged from 01-06: `clears_practical_floor = corrected_delta >= PRACTICAL_FLOOR`, and `test_floor_is_applied_to_the_corrected_delta` still runs at `correction_k == 2` |
| T-01-14b | Tampering | vacuous win on identical candidates | mitigate | Truth 8 now holds through the general path; `test_identical_arms_remain_no_difference_on_measured_statistics` asserts `verdict is not Verdict.WIN` |
| T-01-04 | Repudiation | non-reproducible verdict | mitigate | `pair_seed` is unchanged and content-derived; `ReproducibilityTest` is untouched and must stay green |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed; `pyproject.toml` keeps `dependencies = []`; nothing in this plan adds an import outside the standard library and `arena.*` |
</threat_model>

<verification>
- `uv run python -m unittest -v tests.test_arena_adjudication` — green, under 25 seconds.
- `uv run python -m unittest -v tests.test_arena_boundary` — still green (no new `evaluator` / `starter.` / `experiments.` reference reached `arena/adjudication.py`).
- `uv run python -W error::ResourceWarning -m unittest discover -s tests` — full suite green, no new failures against the 339-test baseline.
- Both mutation checks executed and reverted, with `git diff --stat` confirmed clean afterwards.
</verification>

<success_criteria>
- A candidate regressing HR@10 by `0.030` and MRR by `0.010` while improving MTTC by `4.11` turns is reported as NOT a win, with `hr10_exchange_rate` named in `failed_criteria`.
- A uniform rank-2 to rank-1 promotion over 200 sessions is reported as `win` on a measured `permutation_p` at the Phipson-Smyth floor, not as `no difference` on an asserted `1.0`.
- No field on any adjudication row is assigned a literal; the row can be re-derived from its own printed columns on every path.
- Two identical arms are still `no difference` and never a win, and the same inputs still adjudicate byte-identically twice.
- The Holm-family membership question has a written, tested answer.
</success_criteria>

<output>
Create `.planning/phases/01-measurement-rig-core/01-10-SUMMARY.md` when done.
</output>
