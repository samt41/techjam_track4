---
phase: 01-measurement-rig-core
plan: 15
type: execute
wave: 3
depends_on: ["01-10", "01-11", "01-12", "01-13", "01-14"]
files_modified:
  - arena/leaderboard.py
  - tests/test_arena_leaderboard.py
  - experiments/baselines/leaderboard.json
  - experiments/LEADERBOARD.md
  - experiments/RUNS.md
  - .planning/phases/01-measurement-rig-core/01-06-SUMMARY.md
autonomous: false
gap_closure: true
requirements: [MEAS-01, MEAS-07]

must_haves:
  truths:
    - "The leaderboard report shows TechnicalScore, HR@10, MRR and MTTC as separate columns per scenario as well as overall"
    - "The committed leaderboard reflects the repaired adjudication, the corrected percentile interval and the new per-scenario column"
    - "The committed Markdown regenerates byte-identically from the committed JSON, and re-running the adjudication produces no diff"
    - "Every number quoted in `experiments/RUNS.md` prose matches the regenerated report"
    - "`01-06-SUMMARY.md`'s claim that the degenerate short-circuit was redundant with the general path is corrected on the record rather than deleted"
  artifacts:
    - path: "arena/leaderboard.py"
      provides: "Per-scenario TechnicalScore in the payload and the rendered table, plus the disclosures the new numbers require"
      exports: ["build_leaderboard", "render_markdown", "HOW_TO_READ"]
      contains: "technical_score(scenario.summary)"
    - path: "experiments/baselines/leaderboard.json"
      provides: "Regenerated machine-readable source of truth at R=10,000"
    - path: "experiments/LEADERBOARD.md"
      provides: "Regenerated human- and judge-readable view with the per-scenario TechnicalScore column"
    - path: "experiments/RUNS.md"
      provides: "Prose numbers reconciled with the regenerated report"
    - path: ".planning/phases/01-measurement-rig-core/01-06-SUMMARY.md"
      provides: "The corrected redundancy claim, with what was actually true recorded beside it"
  key_links:
    - from: "arena/leaderboard.py"
      to: "arena/metrics.py"
      via: "technical_score computed from each ScenarioSummary's own MetricSummary"
      pattern: "technical_score\\(scenario.summary\\)"
    - from: "experiments/LEADERBOARD.md"
      to: "experiments/baselines/leaderboard.json"
      via: "render_markdown over the committed payload, byte-identical"
      pattern: "render_markdown"
---

<objective>
Close the last verification gap — the missing per-scenario TechnicalScore column — and
regenerate the committed report so that every number it prints was produced by the
repaired rig.

Purpose: SC1 asks for TechnicalScore, HR@10, MRR and MTTC as separate columns "both
overall and broken out per scenario". Overall is complete; per scenario the composite
column is absent from both the JSON payload and the rendered table. Separately, four
earlier plans in this round moved numbers the committed report prints or the assumptions
it states, so the report as committed no longer describes the rig that exists. SC3's
byte-identical-regeneration property is the phase's strongest evidence that the pipeline
is deterministic and auditable, and it can only be re-established after every numeric
change has landed — which is why this plan is last and depends on all four code plans
plus the leaderboard auditability plan.

Why the code change and the regeneration are one plan rather than two: adding a rendered
column makes `experiments/LEADERBOARD.md` disagree with `render_markdown` the instant it
lands, so `test_the_committed_markdown_matches_the_committed_payload` is red between the
edit and the regeneration. Keeping both inside one plan bounds that window to the gap
between Task 1 and Task 2 instead of leaving it open across a wave boundary. Task 1's
acceptance criteria name the expected failure explicitly; Task 2 closes it.

Output: a report that satisfies SC1 in full, regenerates byte-identically, and whose
prose, JSON, Markdown and phase SUMMARYs all state the same facts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-measurement-rig-core/01-CONTEXT.md
@.planning/phases/01-measurement-rig-core/01-VERIFICATION.md
@.planning/phases/01-measurement-rig-core/01-REVIEW.md
@.planning/phases/01-measurement-rig-core/01-10-PLAN.md
@.planning/phases/01-measurement-rig-core/01-11-PLAN.md
@.planning/phases/01-measurement-rig-core/01-14-PLAN.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Symbols created by **this plan** (new — not pre-existing drift):

| Kind | Name | File |
|------|------|------|
| payload key | `technical_score` (on every `scenario_breakout` row) | `arena/leaderboard.py` |
| rendered column | `TechnicalScore` in the "Per-scenario breakout" table | `arena/leaderboard.py` |
| test method | `test_scenario_rows_carry_a_technical_score`, `test_the_scenario_technical_score_is_bucket_local`, `test_the_scenario_table_renders_a_technical_score_column`, `test_the_committed_payload_carries_a_per_scenario_technical_score` | `tests/test_arena_leaderboard.py` |

`build_leaderboard`, `render_markdown`, `write_leaderboard`, `HOW_TO_READ`,
`LEADERBOARD_SCHEMA_VERSION`, `_cell` and `_table` keep their existing names and
signatures. `arena/metrics.py` is **not** modified: `ScenarioSummary.as_record()` keeps
its current six keys, and the composite is added at the leaderboard's output boundary,
where the other output-only concerns (`efficiency` rounding, fingerprint truncation)
already live.

<tasks>

<task type="auto">
  <name>Task 1: Add the per-scenario TechnicalScore column and the disclosures the round's numbers require</name>
  <files>arena/leaderboard.py, tests/test_arena_leaderboard.py</files>
  <read_first>
    - `arena/leaderboard.py` — the whole file, especially the `arena.metrics` import block at lines 13-20, the `HOW_TO_READ` string at lines 52-131 with its four numbered divergences and its `Verdict vocabulary` / `Precision` / `Efficiency rounding` paragraphs, the scenario row construction at lines 284-292 with its unrounded-sigma comment, the `scenario_rows` format string at lines 422-435, and the `scenario_table` header and alignment tuples at lines 436-440
    - `arena/metrics.py` — `ScenarioSummary` (note it carries `summary: MetricSummary`), `ScenarioSummary.as_record()`, `metric_summary`, `efficiency` and `technical_score`, including the comment explaining that `efficiency` consumes the ALREADY-ROUNDED mttc and returns UNROUNDED
    - `.planning/phases/01-measurement-rig-core/01-VERIFICATION.md` — the YAML frontmatter `gaps:` block, THIRD entry (truth 1 / SC1 / MEAS-01), including its `missing:` line naming `technical_score(scenario.summary)`
    - `.planning/phases/01-measurement-rig-core/01-CONTEXT.md` decisions D-13, D-15, D-17 and D-19
    - `.planning/phases/01-measurement-rig-core/01-14-PLAN.md` Task 2 — the `holm_family_size` / `holm_family_includes_degenerate_arms` keys this task discloses in prose
    - `.planning/phases/01-measurement-rig-core/01-11-PLAN.md` Task 1 — the `(R+1)` percentile convention and the pinned `(249, 9750)` indices this task discloses in prose
    - `experiments/LEADERBOARD.md` lines 123-149 — the current "Per-scenario breakout" heading, its two-line preamble, and the eight-column table this task widens to nine
    - `tests/test_arena_leaderboard.py` — `LeaderboardPayloadTest`, `LeaderboardMarkdownTest` (especially `test_numeric_separator_columns_are_right_aligned` at line 346), and `CommittedLeaderboardTest.test_the_committed_payload_carries_the_anchor_scenario_breakout` at line 463
  </read_first>
  <action>
    **Payload.** In `build_leaderboard`, the scenario row currently constructed as
    `{"fingerprint": entry.fingerprint, **scenario.as_record()}` gains a
    `"technical_score"` key computed as `technical_score(scenario.summary)`. Add the key
    at the leaderboard's output boundary rather than inside
    `arena/metrics.ScenarioSummary.as_record()`, and comment why: `arena/metrics.py` is
    the transcription of the evaluator's own metric chain (its module comment says so),
    and the evaluator emits no per-scenario composite. Adding one there would make the
    transcription claim false and would put a leaderboard presentation choice inside the
    module whose agreement with the evaluator is the phase's validation evidence.
    `technical_score` is already imported at line 19; add no new import.

    The value passes through unrounded by `_cell`'s six-decimal formatting because
    `technical_score` already rounds its own result to 6 dp — the same rule the candidate
    table's `technical_score` follows. Do not add a second `round(...)`.

    **Render.** Widen the "Per-scenario breakout" table from eight columns to nine.
    `TechnicalScore` goes immediately after `MTTC` and before `binomial sigma`, so the
    metric quartet reads in the same order as the Candidates table. The header tuple
    becomes `("Candidate", "Scenario", "n", "HR@10", "MRR", "MTTC", "TechnicalScore", "binomial sigma", "Decision-grade?")`
    and the alignment tuple becomes `("---", "---", "---:", "---:", "---:", "---:", "---:", "---:", "---")` —
    nine entries, with the new column right-aligned like every other numeric column.
    Add the matching `` `{technical_score}` `` placeholder to the `scenario_rows` format
    string, fed by `_cell(item["technical_score"])`.

    **HOW_TO_READ disclosures.** The block's stated purpose is that an apparent
    inconsistency should read as what it is. This round creates three new ones and they
    must all be stated. Add them in the block's existing numbered-then-bolded style,
    keeping the four existing numbered divergences intact and renumbering only if the new
    material is genuinely numbered:

    1. **The per-scenario TechnicalScore does not average to the overall one.** Each
       per-scenario TechnicalScore is computed from that bucket's OWN HR@10, MRR and
       MTTC, so it is the score the bucket would have scored in isolation. It is NOT a
       decomposition of the overall score and the four buckets do not combine to it under
       any weighting, because `Efficiency = clip((11 - mean(MTTC)) / 10, 0, 1)` is a
       function of a mean rather than a mean of per-session values (D-17). A reader who
       n-weights the four bucket scores will get a number that is close to, and not equal
       to, the overall TechnicalScore, and the overall figure is the correct one. Say so
       explicitly, and repeat the D-19 point that per-scenario rows are descriptive
       non-inferiority gates and are never Holm-corrected.
    2. **A degenerate arm still counts toward the Holm family and `correction_k`.** An arm
       whose delta and standard error are both zero remains in the family, because the
       family size is a property of the experimental design and shrinking it after seeing
       which arms turned out degenerate would be a data-dependent family definition.
       `--include` is the a-priori mechanism for a retained record that belongs in the
       report without joining the family. Name the two new `assumptions` keys —
       `holm_family_size` and `holm_family_includes_degenerate_arms` — so a reader can
       check the claim against the payload. Add that every value on every adjudication
       row is now measured: no field is a hard-coded constant on any path, including the
       zero-variance one, and the `is_degenerate` column is descriptive only.
    3. **The 95% interval uses the `(R + 1)` order-statistic convention.** State that the
       lower and upper bounds are the `floor(0.025 * (R + 1))`-th and
       `ceil(0.975 * (R + 1))`-th order statistics of the sorted replicates, which at
       `R = 10,000` are indices `249` and `9750`, that the two bounds are mirror images
       of one another, and that the resulting nominal coverage is at or above 95% at every
       admissible resample count. Add that the previous report's bounds were computed one
       index differently and gave 94.99% coverage with asymmetric tails, so a reader
       comparing this report against the previous one will see the CI move slightly and
       nothing else move with it — `standard_error`, MDD, sigma-hat and every
       winner's-curse quantity are unchanged, because only the interval indices moved.

    **Tests.** Add to `tests/test_arena_leaderboard.py`:
    - `test_scenario_rows_carry_a_technical_score` — every row in a freshly built
      payload's `scenario_breakout` has a `technical_score` key whose value equals
      `technical_score(metric_summary(<that bucket's sessions>))` exactly.
    - `test_the_scenario_technical_score_is_bucket_local` — on a fixture with at least two
      scenario buckets of different sizes, assert that the n-weighted mean of the
      per-scenario TechnicalScores does NOT equal the overall TechnicalScore, and assert
      the two agree to within `0.05`. The inequality is the point of the disclosure; the
      loose bound is the non-vacuity guard that stops the test passing on a nonsense
      value.
    - `test_the_scenario_table_renders_a_technical_score_column` — the rendered
      "Per-scenario breakout" header line contains `TechnicalScore`, its separator row has
      nine cells, and every body row in that table has nine cells.
    - `test_the_committed_payload_carries_a_per_scenario_technical_score` — placed in
      `CommittedLeaderboardTest`: every row of the committed payload's `scenario_breakout`
      has a `technical_score` key, and the four `anchor-legacy` rows carry finite values
      strictly between `0.0` and `1.0`. This test is EXPECTED TO FAIL until Task 2
      regenerates the artifact.

    Do not weaken `test_the_committed_markdown_matches_the_committed_payload`,
    `test_numeric_separator_columns_are_right_aligned`, or
    `test_the_committed_payload_carries_the_anchor_scenario_breakout`. Do not modify
    `arena/metrics.py`.
  </action>
  <acceptance_criteria>
    - `grep -c 'technical_score(scenario.summary)' arena/leaderboard.py` returns `1`.
    - `git diff --name-only` for this task does NOT include `arena/metrics.py`.
    - Every `scenario_breakout` row of a freshly built payload has exactly this 9-key set: `fingerprint`, `scenario_type`, `sample_count`, `hit_rate_at_10`, `mrr`, `mttc`, `technical_score`, `binomial_standard_error`, `decision_grade`. Record the asserted key set in the SUMMARY.
    - `render_markdown` over a freshly built payload emits a per-scenario header line containing `| TechnicalScore |` positioned between `| MTTC |` and `| binomial sigma |`, and the alignment row for that table has 9 cells of which the 7th is `---:`.
    - `grep -c 'TechnicalScore' arena/leaderboard.py` returns at least `3` (candidate header, scenario header, and at least one HOW_TO_READ mention).
    - `HOW_TO_READ` contains all three new disclosure anchors: the substrings `holm_family_size`, `holm_family_includes_degenerate_arms`, and `(R + 1)`.
    - `uv run python -m unittest -v tests.test_arena_leaderboard` reports EXACTLY TWO failures at the end of this task, both expected and both caused by the committed artifact not yet being regenerated: `test_the_committed_markdown_matches_the_committed_payload` and `test_the_committed_payload_carries_a_per_scenario_technical_score`. Record both names in the SUMMARY. Any THIRD failure is a real defect and must be fixed before Task 2. Do not weaken, skip, or `expectedFailure` either test.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m unittest -v tests.test_arena_leaderboard 2>&1 | tail -40</automated>
  </verify>
  <done>The per-scenario table carries all four metrics, and every number this round moved has a written disclosure attached before the artifact that prints it is regenerated.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the committed report at production scale and re-prove byte-identical regeneration</name>
  <files>experiments/baselines/leaderboard.json, experiments/LEADERBOARD.md</files>
  <read_first>
    - `arena/run_arena.py` — the `adjudicate` subcommand, its `--baseline` / `--candidate` / `--include` / `--output-root` arguments, and the comment at lines 186-191 explaining which records are report-only
    - `arena/leaderboard.py` as amended by Task 1 — `write_leaderboard`, `LEADERBOARD_JSON_PATH`, `LEADERBOARD_MARKDOWN_PATH`
    - `experiments/baselines/leaderboard.json` — the committed payload: 5 candidates, 2 adjudication rows, and the `assumptions` block
    - `.planning/phases/01-measurement-rig-core/01-09-SUMMARY.md` § "Verification" — the reproducibility gate that plan recorded (`re-ran adjudication, git diff --quiet exits 0`), which is the property this task re-establishes
    - `.planning/phases/01-measurement-rig-core/01-VERIFICATION.md` — truth 3 (SC3) and truth 10, both currently VERIFIED and both of which must stay verified after regeneration
    - `arena/statistics.py` as amended by plan 01-11 — `RESAMPLE_COUNT = 10_000` is the default and no CLI flag exposes it (D-24, T-01-20)
  </read_first>
  <action>
    Regenerate both committed artifacts from the committed records, at the production
    resample count, using the exact five-record invocation that produced them:

    `uv run python -m arena.run_arena adjudicate --baseline experiments/baselines/run-a --candidate experiments/baselines/run-b --candidate experiments/baselines/run-c --include experiments/baselines/anchor-legacy --include experiments/baselines/synthetic-promote-10`

    Run it from the repository root. `--include` carries `anchor-legacy` and
    `synthetic-promote-10` into the candidate, curve and scenario tables without entering
    `adjudicate()`, which is why the report has five candidates and two adjudication rows.
    Pass no resample flag; none exists, and the default `RESAMPLE_COUNT = 10_000` is what
    T-01-20 requires. Expect the command to take longer than the previous round's roughly
    25 seconds: the `exploration-tail-only` arm now runs a real 10,000-replicate
    permutation instead of short-circuiting.

    Then re-establish the two properties this regeneration puts at risk:
    - **Reproducibility gate.** Run the identical command a SECOND time and assert
      `git diff --quiet -- experiments/baselines/leaderboard.json experiments/LEADERBOARD.md`
      exits `0`. Two production runs of the adjudication on the same inputs must produce
      byte-identical artifacts; seeds derive from candidate fingerprints via `pair_seed`
      and never from the clock.
    - **Render identity.** Confirm `experiments/LEADERBOARD.md` equals `render_markdown`
      over the committed `experiments/baselines/leaderboard.json`, which is exactly what
      `test_the_committed_markdown_matches_the_committed_payload` asserts.

    Record in the SUMMARY, as a before/after table, every value that moved on the two
    adjudication rows and on the `assumptions` block, with the plan that caused each
    move. The expected set, to be confirmed against what actually happens rather than
    assumed:
    - `fallback-lexical`: `ci_lower` and `ci_upper` move by one order statistic each
      (plan 01-11). `delta`, `standard_error`, `permutation_p`, `holm_p`,
      `minimum_detectable_difference`, `correction_k`, `expected_max_of_k`,
      `corrected_delta`, `clears_practical_floor`, `failed_criteria` and `verdict` should
      NOT move — the SE is unaffected by the index change and the exchange-rate criterion
      does not fire on a row whose `hit_rate_delta` is `+0.005`.
    - `exploration-tail-only`: `permutation_p` and `minimum_detectable_difference` are now
      MEASURED rather than asserted (plan 01-10). Both are expected to land on the same
      values they previously asserted — `1.0` and `0.0` — because the arm's sessions are
      byte-identical to the baseline's, so every sign-flip ties the observed statistic and
      the Phipson-Smyth p is exactly `1.0`. Its `verdict` should remain `no difference`
      and its `failed_criteria` should remain `["holm_significance", "practical_floor"]`.
      If any of these DOES move, that is a finding to record and investigate, not a
      failure to paper over.
    - Both rows gain an `is_degenerate` key (plan 01-10). The `assumptions` block gains
      `holm_family_size` and `holm_family_includes_degenerate_arms` (plan 01-14). All
      twenty `scenario_breakout` rows gain `technical_score` (Task 1).

    Do NOT hand-edit either artifact. Both are generated files and
    `experiments/LEADERBOARD.md` says so in its own header. If a number looks wrong, fix
    the code and regenerate.

    Do not re-run any evaluation. No `Agent` is constructed by this task; the four
    `sessions.jsonl` records are committed and unchanged, and `git status --porcelain`
    must show no modification under `experiments/baselines/*/`.
  </action>
  <acceptance_criteria>
    - `uv run python -W error::ResourceWarning -m unittest discover -s tests` passes the FULL suite with zero failures, including the two tests that were expected-red at the end of Task 1.
    - Running the regeneration command a second time leaves `git diff --quiet -- experiments/baselines/leaderboard.json experiments/LEADERBOARD.md` exiting `0`.
    - `python -c "import json,pathlib; p=json.loads(pathlib.Path('experiments/baselines/leaderboard.json').read_text()); print(len(p['candidates']), len(p['adjudication']), len(p['scenario_breakout']), p['assumptions']['resample_count'], p['assumptions']['holm_family_size'], p['assumptions']['holm_family_includes_degenerate_arms'])"` prints `5 2 20 10000 2 True`.
    - Every row of the committed payload's `adjudication` carries an `is_degenerate` key, and exactly one of the two is `True` (`exploration-tail-only`).
    - Every one of the 20 committed `scenario_breakout` rows carries a `technical_score` key with a value strictly between `0.0` and `1.0`.
    - The committed payload still reports the MEAS-16 anchor exactly: the `anchor-legacy` candidate row still reads `hit_rate_at_10 == 0.92`, `mrr == 0.524466`, `mttc == 3.425`, `efficiency == 0.7575`, `technical_score == 0.76884`, `sample_count == 200`, and its four scenario HR@10 values are still `[0.9, 0.95, 0.9, 0.9]`.
    - `git status --porcelain experiments/baselines/` lists only `leaderboard.json` as modified; no `summary.json` and no `sessions.jsonl` changed.
    - `grep -cF '| MTTC | TechnicalScore | binomial sigma |' experiments/LEADERBOARD.md` returns `1` — the widened per-scenario header, matched precisely so the Candidates table's own `TechnicalScore` header cell cannot satisfy it — and every per-scenario body row in the file has 9 cells.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -W error::ResourceWarning -m unittest discover -s tests</automated>
  </verify>
  <done>The committed report was produced by the repaired rig at 10,000 replicates, regenerates byte-identically, and still reproduces the MEAS-16 anchor to the digit.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the prose record with the regenerated numbers</name>
  <files>experiments/RUNS.md, .planning/phases/01-measurement-rig-core/01-06-SUMMARY.md</files>
  <read_first>
    - `experiments/RUNS.md` lines 1-70 — the "Superseded as the system of record" section, the "Two measured findings" bullets with their quoted CI, permutation p, Holm p, MDD, sigma-hat and corrected delta, and the "Which numbers are current" paragraph
    - `experiments/baselines/leaderboard.json` as regenerated by Task 2 — the authoritative source for every number the prose quotes
    - `.planning/phases/01-measurement-rig-core/01-06-SUMMARY.md` — the whole file, and specifically line 153, the "Issues Encountered" bullet beginning "**The degenerate short-circuit is redundant with the general path, deliberately.**"
    - `.planning/phases/01-measurement-rig-core/01-VERIFICATION.md` — the second gap entry's `artifacts:` list, which names `01-06-SUMMARY.md:153` as an artifact with an issue, and the § "Gaps Summary" paragraph explaining why a SUMMARY-versus-code divergence is exactly what this verification exists to catch
    - `.planning/phases/01-measurement-rig-core/01-CONTEXT.md` decision D-05 — `experiments/RUNS.md` is not rewritten; it gains corrections and nothing is deleted from the historical prose below the pointer section
  </read_first>
  <action>
    **`experiments/RUNS.md`.** Update only the numbers and claims that the regeneration
    changed, in the pointer section at lines 5-67. Everything below the
    `### Historical (pre-SQLite, in-memory catalog)` heading is untouched — D-05 keeps
    that prose intact and this task must not delete a line of it.

    - In the `ΔTS(A, B)` bullet, replace the quoted 95% CI with the regenerated bounds
      read from `experiments/baselines/leaderboard.json`, and leave `permutation_p`,
      `holm_p`, MDD, sigma-hat and corrected `ΔTS` at their existing values unless the
      regenerated payload disagrees — in which case update them and say so.
    - In the `ΔTS(A, C)` bullet, replace the clause stating that "the rig reports them
      through its zero-variance guard rather than as a detectable difference". The guard
      no longer sets any value. State instead that both arms' 200 session outcomes are
      byte-identical, that every quantity on the row — including the permutation p and the
      MDD — is now MEASURED on the general path rather than asserted by a special case,
      and that the measured permutation p is exactly `1.0` because every sign-flip
      assignment of two identical arms ties the observed statistic.
    - Add a short paragraph, in the same voice as the existing "Which numbers are
      current" one, recording that the report was regenerated after this gap-closure
      round and naming what moved and why: the corrected `(R + 1)` percentile convention
      moved the interval bounds; the removal of the zero-variance short-circuit made two
      previously asserted fields measured; the adjudication row gained `is_degenerate`;
      the assumptions block gained `holm_family_size` and
      `holm_family_includes_degenerate_arms`; the per-scenario table gained a
      TechnicalScore column. State that `correction_k` remains `2` and that the deltas are
      unchanged, since the session outcomes did not move.

    **`.planning/phases/01-measurement-rig-core/01-06-SUMMARY.md`.** Correct line 153.
    Do NOT delete the bullet — it is the exact class of SUMMARY-versus-code divergence
    this verification exists to catch, and preserving the correction is part of the
    auditability deliverable. Rewrite it so it records three things in order:
    1. What was claimed at the time: that the short-circuit was redundant with the general
       path, and that every value it set was exactly what the general path produces when
       SE is `0.0`.
    2. That the claim was FALSE as shipped, and how it was disproven — executed
       reproduction against the checked-in code on a uniform rank-2 to rank-1 promotion
       over 200 sessions. The guard produced `verdict = no difference` with
       `permutation_p = 1.0` asserted and `clears_practical_floor = False` beside a
       `corrected_delta` of `0.15`; the general path on the identical fixture produced a
       measured `permutation_p` of `0.0004997501249375312`, `clears_practical_floor = True`,
       `failed_criteria = ()` and `verdict = WIN`. The guard did not agree with the general
       path — it inverted the verdict. The error in the original reasoning was treating
       "the delta does not move under resampling" as "there is no delta": bootstrap SE is
       zero for ANY exactly-uniform per-session improvement, not only for identical arms.
    3. What gap-closure plan 01-10 changed: degeneracy is now conditioned on the delta as
       well as the standard error, the short-circuit is gone, every field is measured on
       the general path, and the narrowed flag survives only as the descriptive
       `is_degenerate` column. Note that the claim's ONE correct half is now true by
       construction rather than by assertion — the general path does produce
       `no difference` for two identical arms, which is what truth 8 requires.

    Mark the correction visibly — a dated `**Correction (gap closure, plan 01-10):**`
    prefix on the rewritten bullet is enough — so a reader can see that the record was
    amended rather than silently rewritten. Do not touch any other line of that SUMMARY;
    in particular leave its frontmatter, its `requirements-completed`, its task commits
    and its threat table alone.
  </action>
  <acceptance_criteria>
    - `grep -c 'redundant with the general path' .planning/phases/01-measurement-rig-core/01-06-SUMMARY.md` returns `1` and the surrounding bullet also contains the substring `Correction (gap closure, plan 01-10)` — the original claim is quoted, not erased.
    - `grep -c '0.0004997501249375312' .planning/phases/01-measurement-rig-core/01-06-SUMMARY.md` returns `1`.
    - `git diff --numstat -- .planning/phases/01-measurement-rig-core/01-06-SUMMARY.md` shows deletions confined to the single corrected bullet; the file's frontmatter, `## Task Commits`, `## Verification Results` and `## Threat Flags` sections are byte-identical.
    - Every numeric literal quoted in the `ΔTS(A, B)` and `ΔTS(A, C)` bullets of `experiments/RUNS.md` matches the corresponding field in `experiments/baselines/leaderboard.json` after rounding to the precision printed there. Verify this by reading the JSON, not by memory, and record the comparison in the SUMMARY.
    - `git diff --numstat -- experiments/RUNS.md` shows deletions only within lines 5-67; `grep -c '### Historical (pre-SQLite, in-memory catalog)' experiments/RUNS.md` returns `1` and every heading below it is unchanged.
    - `grep -c 'zero-variance guard' experiments/RUNS.md` returns `0` — the superseded mechanism claim is gone from the prose.
    - `uv run python -W error::ResourceWarning -m unittest discover -s tests` still passes the full suite (this task changes no code, so the suite must be green throughout).
  </acceptance_criteria>
  <verify>
    <automated>uv run python -W error::ResourceWarning -m unittest discover -s tests</automated>
  </verify>
  <done>The prose, the JSON, the Markdown and the phase SUMMARYs all state the same facts, and the one documented claim that was false is corrected on the record with its disproof attached.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator sign-off on the regenerated report</name>
  <files>experiments/baselines/leaderboard.json, experiments/LEADERBOARD.md, experiments/RUNS.md</files>
  <read_first>
    - `experiments/LEADERBOARD.md` as regenerated by Task 2
    - `experiments/baselines/leaderboard.json` as regenerated by Task 2
    - `experiments/RUNS.md` lines 5-70 as amended by Task 3
    - The before/after table recorded in this plan's SUMMARY
  </read_first>
  <action>
    Pause. Present the six verification steps below to the operator and wait for the
    resume signal before committing the regenerated artifacts. Automate nothing further:
    every mechanical check has already run in Tasks 1-3, and what remains is a judgement
    call on whether the regenerated report reads as intended and whether anything moved
    that the SUMMARY did not predict. Do not commit `experiments/baselines/leaderboard.json`,
    `experiments/LEADERBOARD.md` or `experiments/RUNS.md` until the operator approves.
  </action>
  <what-built>
    The committed `experiments/baselines/leaderboard.json` and `experiments/LEADERBOARD.md`
    have been regenerated at 10,000 replicates from the four committed records, using the
    repaired adjudication (both blockers closed), the corrected `(R + 1)` percentile
    convention, the new per-scenario TechnicalScore column, and the new assumptions
    disclosures. `experiments/RUNS.md` prose and `01-06-SUMMARY.md:153` have been
    reconciled with the new numbers. The full test suite is green and a second
    regeneration produced no diff.
  </what-built>
  <how-to-verify>
    1. Open `experiments/LEADERBOARD.md`. Confirm the "Per-scenario breakout" table now
       has a `TechnicalScore` column between `MTTC` and `binomial sigma`, and that the
       twenty rows carry plausible values.
    2. In the "Pairwise adjudication" table, confirm `exploration-tail-only` still reads
       verdict `no difference` and `fallback-lexical` still reads `not detectable`, and
       that neither row reads `win`.
    3. In the "How to read this report" section, confirm the three new disclosures are
       present and read as intended: the per-scenario TechnicalScore not averaging to the
       overall one, the degenerate arm still counting toward the Holm family, and the
       `(R + 1)` interval convention.
    4. Run `git diff experiments/RUNS.md` and confirm nothing below the
       `### Historical (pre-SQLite, in-memory catalog)` heading was deleted.
    5. Run `uv run python -m arena.run_arena adjudicate --baseline experiments/baselines/run-a --candidate experiments/baselines/run-b --candidate experiments/baselines/run-c --include experiments/baselines/anchor-legacy --include experiments/baselines/synthetic-promote-10`
       once more and confirm `git status --porcelain experiments/` reports nothing.
    6. Confirm the before/after table in the plan SUMMARY accounts for every value that
       moved, and that nothing moved which the SUMMARY did not predict.
  </how-to-verify>
  <acceptance_criteria>
    - The operator has replied with the resume signal before any of the three artifacts is committed.
    - If the operator describes an issue instead of approving, the fix lands in code and the artifacts are regenerated — neither file is hand-edited under any circumstance.
    - `git log --oneline -1 -- experiments/baselines/leaderboard.json` shows no commit for this plan until after approval.
  </acceptance_criteria>
  <resume-signal>Type "approved" to commit the regenerated report, or describe what looks wrong</resume-signal>
  <verify>
    <human-check>Operator confirms the six steps above and replies with the resume signal</human-check>
  </verify>
  <done>The operator has read the regenerated report and approved committing it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| regenerated artifact → committed evidence | `experiments/baselines/leaderboard.json` and `experiments/LEADERBOARD.md` are git-tracked evidence a judge may read; a report regenerated at a reduced resample count, or hand-edited, would be indistinguishable from an honest one without the recorded `resample_count` and the render-identity check |
| prose record → reader's belief | `experiments/RUNS.md` and the phase SUMMARYs are read as history; a number left quoting a superseded run is a claim the artifacts contradict |
| rendered report → judge's reading | A per-scenario composite a reader might average into the overall score is a misreading surface unless the report says it does not decompose that way |

Not applicable, with reasons: no network endpoint, no credential, no authentication or
access-control surface, no cryptographic storage claim, no untrusted external input (the
inputs are four committed, git-tracked record directories), and no `Agent` or evaluator
invocation — this plan re-reads existing session outcomes and never re-runs an
evaluation. The one destructive filesystem concern, `write_leaderboard`'s
non-atomic two-file write (WR-12), was not selected by the operator for this round and
is not addressed here; it is mitigated in practice by the render-identity test, which
detects a JSON/Markdown split after the fact.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-35 | Repudiation | a committed report describing a rig that no longer exists | mitigate | Both artifacts regenerated from the committed records after every numeric change has landed; a second regeneration must leave `git diff --quiet` exiting `0` |
| T-01-36 | Repudiation | a per-scenario composite misread as a decomposition of the overall score | mitigate | `HOW_TO_READ` states that the bucket score is computed from the bucket's own metrics and does not combine to the overall one under any weighting (D-17); `test_the_scenario_technical_score_is_bucket_local` asserts the inequality |
| T-01-16 | Tampering | a hand-edited or drifted rendered report | mitigate | Unchanged: the file's own header forbids hand-editing, and `test_the_committed_markdown_matches_the_committed_payload` must be green at the end of Task 2 |
| T-01-20 | Tampering | a committed report generated at a reduced resample count | mitigate | No CLI flag exposes the resample count (D-24); `assumptions.resample_count` is derived from the rows and asserted to be `10000` |
| T-01-21 | Tampering | destructive edit to the irreplaceable `RUNS.md` prose | mitigate | Edits confined to lines 5-67; a `git diff --numstat` acceptance criterion plus a heading check assert nothing below the historical heading was removed (D-05) |
| T-01-05 | Tampering | a regenerated artifact silently falling out of the committed set | mitigate | `git status --porcelain experiments/baselines/` is checked, and the `!experiments/baselines/` negation in `.gitignore` is unchanged |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed; `pyproject.toml` keeps `dependencies = []`; this plan adds no import at all |
</threat_model>

<verification>
- End of Task 1: `uv run python -m unittest -v tests.test_arena_leaderboard` reports exactly two named, expected failures and no third.
- End of Task 2: `uv run python -W error::ResourceWarning -m unittest discover -s tests` — full suite green, zero failures.
- End of Task 2: second regeneration leaves `git diff --quiet -- experiments/baselines/leaderboard.json experiments/LEADERBOARD.md` exiting `0`.
- End of Task 3: full suite still green; `git diff --numstat` on `experiments/RUNS.md` and `01-06-SUMMARY.md` confirms deletions are confined to the sections named in the acceptance criteria.
- Operator sign-off recorded before the regenerated artifacts are committed.
</verification>

<success_criteria>
- SC1 is satisfied in full: TechnicalScore, HR@10, MRR and MTTC appear as separate columns both overall and per scenario, in the payload and in the rendered report.
- SC3's byte-identical regeneration property is re-established and re-proven against the repaired rig.
- The MEAS-16 anchor still reproduces to the digit in the regenerated payload.
- `experiments/RUNS.md` quotes no number the regenerated report contradicts, and no line of its historical prose was deleted.
- The false claim at `01-06-SUMMARY.md:153` is corrected on the record, with the original claim quoted and the disproof attached.
</success_criteria>

<output>
Create `.planning/phases/01-measurement-rig-core/01-15-SUMMARY.md` when done.
</output>
