---
phase: 01-measurement-rig-core
plan: 09
type: execute
wave: 7
depends_on: ["01-08"]
files_modified:
  - experiments/baselines/run-a/sessions.jsonl
  - experiments/baselines/run-a/summary.json
  - experiments/baselines/run-b/sessions.jsonl
  - experiments/baselines/run-b/summary.json
  - experiments/baselines/run-c/sessions.jsonl
  - experiments/baselines/run-c/summary.json
  - experiments/baselines/leaderboard.json
  - experiments/LEADERBOARD.md
  - experiments/RUNS.md
autonomous: false
requirements: [MEAS-03, MEAS-16]

must_haves:
  truths:
    - "A run produced by the arena reproduces the retained RUNS.md row and agrees with the rescued anchor record on all six aggregates"
    - "Two independent code paths — experiments/run_public.py and arena/ — agree on the same anchor numbers"
    - "Three provenance-carrying candidate records are committed and permanently tracked"
    - "The adjudication of A against B and A against C produces a reproducible verdict and an MDD, with no significance outcome assumed in advance"
    - "experiments/RUNS.md points at the leaderboard and is otherwise unchanged"
  artifacts:
    - path: "experiments/baselines/run-a/summary.json"
      provides: "The MEAS-16 anchor with full provenance"
      contains: "fingerprint"
    - path: "experiments/baselines/run-b/summary.json"
      provides: "The lexical-mode ablation arm (D-02 run B)"
    - path: "experiments/baselines/run-c/summary.json"
      provides: "The exploration ablation arm (D-02 run C)"
    - path: "experiments/LEADERBOARD.md"
      provides: "The final Phase 1 report over three real candidates"
    - path: "experiments/RUNS.md"
      provides: "A pointer to the leaderboard, with all existing prose intact"
      contains: "LEADERBOARD.md"
  key_links:
    - from: "experiments/baselines/run-a/summary.json"
      to: "experiments/baselines/anchor-legacy/summary.json"
      via: "six-aggregate agreement between two independent code paths"
      pattern: "recommended_technical_score"
    - from: "experiments/RUNS.md"
      to: "experiments/LEADERBOARD.md"
      via: "an additive pointer section (D-05)"
      pattern: "LEADERBOARD\\.md"
---

<objective>
Run the three D-02 candidates through the arena, commit their provenance-carrying
records, adjudicate them, and close the phase with a report generated from real
runs rather than from a rescued file.

Purpose: MEAS-16 and ROADMAP Success Criterion 3. Run A is the reproduction anchor
— if the arena's own run does not reproduce `0.92 / 0.524466 / 3.425 / 0.76884`
then either the rig or the historical row is wrong, and the phase does not proceed
until that is resolved. Runs B and C exercise the adjudication end to end on real
paired data. Two independent code paths agreeing on the anchor is the evidence, not
an accident (D-06).

This is the only plan in the phase that needs the 580 MB artifact and roughly ten
minutes of wall clock. Everything it depends on is already unit-proven.

Output: three committed baseline records, the final leaderboard, an additive
pointer in `experiments/RUNS.md`, and two recorded findings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-measurement-rig-core/01-CONTEXT.md
@.planning/phases/01-measurement-rig-core/01-RESEARCH.md
@.planning/phases/01-measurement-rig-core/01-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Paths created by **this plan** (new — not pre-existing drift):
`experiments/baselines/run-a/`, `experiments/baselines/run-b/`,
`experiments/baselines/run-c/`, each containing `sessions.jsonl` and
`summary.json`; a regenerated `experiments/baselines/leaderboard.json` and
`experiments/LEADERBOARD.md`; and an additive pointer section in
`experiments/RUNS.md`.

The full phase artifact list is in `01-01-PLAN.md` § "Artifacts this phase produces".

<tasks>

<task type="auto">
  <name>Task 1: Run A through the arena and cross-validate the MEAS-16 anchor</name>
  <files>experiments/baselines/run-a/sessions.jsonl, experiments/baselines/run-a/summary.json</files>
  <read_first>
    - `arena/run_arena.py` and `arena/arena.py` (from plan 01-08)
    - `experiments/baselines/anchor-legacy/summary.json` (the rescued aggregates this run must agree with)
    - `experiments/RUNS.md:62-80` — the retained current-HEAD row and its scenario HitRate@10 line
    - `LOCAL_ENVIRONMENT.md` — the FTS5 verification command and the artifact build instructions, in case the artifact is absent
    - `.planning/phases/01-measurement-rig-core/01-RESEARCH.md` § "Environment Availability" (artifact present at 581,844,992 bytes; run A approximately 190 s)
  </read_first>
  <action>
    Confirm the prerequisites first, and stop with a clear message rather than a
    long failing run if any is missing: `data/catalog.jsonl` present,
    `data/catalog.artifacts/catalog.sqlite3` present, `data/public_set.jsonl`
    present, and the CPython SQLite build reporting FTS5 available (the check
    command is in `LOCAL_ENVIRONMENT.md`).

    Then execute run A:
    `uv run python -m arena.run_arena run --run-id run-a --name "baseline-auto-disabled" --lexical-mode auto --exploration disabled`.
    Budget roughly 190 seconds. If the command approaches a tool timeout, re-issue
    it as a background invocation rather than shortening the run — a truncated
    evaluation is worse than a slow one.

    When it completes, verify the published `experiments/baselines/run-a/summary.json`
    against three independent references, in this order:

    1. **The exact 6 dp anchor.** `hit_rate_at_10 == 0.92`, `mrr == 0.524466`,
       `mttc == 3.425`, `efficiency == 0.7575`,
       `recommended_technical_score == 0.76884`, `sample_count == 200`.
    2. **The `experiments/RUNS.md` retained row, only after explicit rounding.**
       `round(mrr, 4) == 0.5245` and
       `round(recommended_technical_score, 4) == 0.7688`. Do NOT write an
       exact-equality assertion against the 4 dp figures: `RUNS.md`'s
       `0.5245` / `0.7688` pair is not self-consistent when read as exact, because
       `0.5*0.92 + 0.3*0.5245 + 0.2*0.7575` is `0.76885`, which displays as
       `0.7689`.
    3. **The rescued record.** All six aggregates and all four scenario summaries
       must equal `experiments/baselines/anchor-legacy/summary.json`. That agreement
       is the two-independent-code-paths evidence D-06 exists to produce, since the
       rescued record came from `evaluator/local_evaluator.py`'s own CLI output and
       this one came through `arena/`.

    Also verify the scenario HitRate@10 line from `RUNS.md`: boundary `0.90`,
    browsing `0.95`, buying `0.90`, intent_override `0.90`, and the per-scenario MRR
    and MTTC `0.404444 / 3.6`, `0.527862 / 3.125`, `0.464296 / 3.2875`,
    `0.715873 / 4.533333` (MEAS-03 recovered from a committed record, agent not
    re-invoked).

    If ANY of these disagrees, stop and report rather than adjusting anything. A
    disagreement means either the rig or the historical row is wrong, and D-01 Layer
    2 makes resolving that a precondition for the rest of the phase.

    Commit `experiments/baselines/run-a/sessions.jsonl` and `summary.json`, and
    confirm git tracks them.
  </action>
  <acceptance_criteria>
    - `experiments/baselines/run-a/summary.json` exists with `sample_count == 200` and exactly `hit_rate_at_10 == 0.92`, `mrr == 0.524466`, `mttc == 3.425`, `efficiency == 0.7575`, `recommended_technical_score == 0.76884`.
    - `round(mrr, 4) == 0.5245` and `round(recommended_technical_score, 4) == 0.7688`.
    - Every aggregate and every scenario summary equals the corresponding value in `experiments/baselines/anchor-legacy/summary.json`.
    - `summary.json` contains a 64-hex `fingerprint`, a 40-hex or `"unknown_revision"` `code_revision`, a boolean `code_revision_dirty`, 64-hex `catalog_sha256` and `dataset_sha256`, `provenance_complete: true`, and `overrides` equal to `{"exploration": "disabled", "lexical_mode": "auto"}`.
    - `experiments/baselines/run-a/sessions.jsonl` has exactly 200 lines.
    - `git check-ignore -q experiments/baselines/run-a/summary.json` exits **1**.
    - `uv run python -m unittest -v tests.test_arena_metrics` still passes.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "import json,pathlib; a=json.loads(pathlib.Path('experiments/baselines/run-a/summary.json').read_text()); b=json.loads(pathlib.Path('experiments/baselines/anchor-legacy/summary.json').read_text()); k=('sample_count','hit_rate_at_10','mrr','mttc','efficiency','recommended_technical_score'); assert [a[x] for x in k]==[200,0.92,0.524466,3.425,0.7575,0.76884], [a[x] for x in k]; assert all(a[x]==b[x] for x in k); assert a['scenario_metrics']==b['scenario_metrics']; assert len(a['fingerprint'])==64; print('MEAS-16 anchor reproduced by two independent paths')"</automated>
  </verify>
  <done>The arena's own run reproduces the retained historical row to six decimal places and agrees with the independently produced rescued record on every aggregate.</done>
</task>

<task type="auto">
  <name>Task 2: Run B and C, adjudicate all three, and point RUNS.md at the leaderboard</name>
  <files>experiments/baselines/run-b/sessions.jsonl, experiments/baselines/run-b/summary.json, experiments/baselines/run-c/sessions.jsonl, experiments/baselines/run-c/summary.json, experiments/baselines/leaderboard.json, experiments/LEADERBOARD.md, experiments/RUNS.md</files>
  <read_first>
    - `experiments/baselines/run-a/summary.json` (from Task 1)
    - `experiments/RUNS.md` in full (136 lines) — the structure, the right-aligned table convention, and the superseded-HEAD section that produced the `0.75 / 0.599` figure
    - `arena/run_arena.py` (`adjudicate` subcommand)
    - `.planning/phases/01-measurement-rig-core/01-CONTEXT.md` decisions D-02, D-03, D-05
    - `.planning/phases/01-measurement-rig-core/01-RESEARCH.md` § "Pitfall 7: Run B is not the large-effect control D-02 assumes"
  </read_first>
  <action>
    Execute run B:
    `uv run python -m arena.run_arena run --run-id run-b --name "fallback-lexical" --lexical-mode fallback --exploration disabled`.
    Budget this one generously. `RUNS.md` records superseded-HEAD FTS runs at
    747-800 s while current-HEAD runs take about 190 s, so the scaling of the TF-IDF
    posting path at current HEAD is not directly transferable. Treat a long runtime
    as expected rather than as a hang; use a background invocation if a foreground
    call would time out.

    Execute run C:
    `uv run python -m arena.run_arena run --run-id run-c --name "exploration-tail-only" --lexical-mode auto --exploration tail-only`.

    Commit both records. Then adjudicate:
    `uv run python -m arena.run_arena adjudicate --baseline experiments/baselines/run-a --candidate experiments/baselines/run-b --candidate experiments/baselines/run-c`.

    **Record whatever the deltas are. Assert nothing about their significance.**
    D-03's measure-don't-assume rule applies to run B as well as run C, because
    D-02's expected `ΔTS ≈ 0.17` for run B is a cross-HEAD artifact: the
    `0.75 / 0.599` figure comes from the superseded `RUNS.md` section at HEAD
    `e76b3ab`, where the FTS baseline was `0.76 / 0.609233`, making the same-HEAD
    lexical-mode effect `0.0102` — which `RUNS.md` itself describes as
    "near-parity". Run B is therefore very likely a SMALL effect at current HEAD.
    What must hold is INTERNAL CONSISTENCY, not a particular outcome: the verdict,
    the CI, the MDD and the Holm-adjusted p must agree with each other, and the
    whole adjudication must reproduce byte-identically on a second invocation.

    Write the two findings into the plan summary and into a short additive
    paragraph in `experiments/RUNS.md`: the measured `ΔTS(A, B)` with its CI, p,
    MDD and verdict, and the same for `ΔTS(A, C)`. If run C turns out non-null at
    current HEAD, that is a finding to record, not a failure of the rig.

    Then edit `experiments/RUNS.md` ADDITIVELY ONLY. D-05 forbids rewriting it: it
    carries irreplaceable prose evidence — the exploration ablation, the two-run
    byte-determinism verification, the forced-fallback verification, and the
    public-ceiling miss audit. Delete nothing and renumber nothing. Add one new
    section near the top, after the opening paragraph, matching the file's existing
    `###`-heading and prose-caveat style, that: points at
    `experiments/LEADERBOARD.md` as the generated report and
    `experiments/baselines/leaderboard.json` as its source of truth; states that
    from Phase 1 onward a "retained historical row" means a committed file under
    `experiments/baselines/` rather than a number in prose; notes that this file's
    aggregates are recorded to 4 decimal places while the leaderboard prints 6, so
    the two agree only after rounding; and records the two new findings above.

    Finally, regenerate the adjudication a second time and assert
    `git diff --quiet -- experiments/baselines/leaderboard.json experiments/LEADERBOARD.md`
    exits 0. That is the D-24 reproducibility gate applied to the real artifacts.
  </action>
  <acceptance_criteria>
    - `experiments/baselines/run-b/sessions.jsonl` and `experiments/baselines/run-c/sessions.jsonl` each have exactly 200 lines, and both `summary.json` files carry a 64-hex `fingerprint`, `provenance_complete: true`, and `overrides` equal to `{"exploration": "disabled", "lexical_mode": "fallback"}` and `{"exploration": "tail-only", "lexical_mode": "auto"}` respectively.
    - The three fingerprints for run-a, run-b and run-c are pairwise distinct.
    - `experiments/baselines/leaderboard.json` contains four candidate entries (anchor-legacy, run-a, run-b, run-c) sorted by `technical_score` descending with an ascending `fingerprint` tie-break, and an `adjudication` array with exactly two rows whose `baseline_fingerprint` is run-a's.
    - Both adjudication rows carry non-null `delta`, `ci_lower`, `ci_upper`, `standard_error`, `permutation_p`, `holm_p`, `minimum_detectable_difference`, `candidate_count == 2`, `correction_k == 2`, `expected_max_of_k`, `corrected_delta`, `verdict` and `failed_criteria`.
    - Internal consistency holds on both rows: `ci_lower <= delta <= ci_upper`; `minimum_detectable_difference` equals `2.801585218112968 * standard_error` to 12 places; `verdict == "win"` if and only if `failed_criteria` is empty; and `verdict == "not detectable"` implies `abs(delta) < minimum_detectable_difference`.
    - No file in this plan asserts a specific significance outcome or a specific delta magnitude for A-vs-B or A-vs-C.
    - `experiments/RUNS.md` still contains every heading it had before the edit (`git diff --numstat -- experiments/RUNS.md` reports `0` in the deletions column).
    - `experiments/RUNS.md` contains the substring `LEADERBOARD.md`.
    - Re-running the adjudication leaves `git diff --quiet -- experiments/baselines/leaderboard.json experiments/LEADERBOARD.md` exiting 0.
    - `git check-ignore -q experiments/baselines/run-b/summary.json` exits **1**.
    - `uv run python -W error::ResourceWarning -m unittest -v` passes the full suite.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "import json,pathlib; p=json.loads(pathlib.Path('experiments/baselines/leaderboard.json').read_text()); rows=p['adjudication']; assert len(rows)==2, len(rows); [((r['ci_lower']<=r['delta']<=r['ci_upper']) and abs(r['minimum_detectable_difference']-2.801585218112968*r['standard_error'])<1e-12 and ((r['verdict']=='win')==(not r['failed_criteria']))) or (_ for _ in ()).throw(AssertionError(r)) for r in rows]; print('adjudication internally consistent')"</automated>
  </verify>
  <done>Three real candidates are committed, adjudicated reproducibly, and reported, with the outcome measured rather than assumed and `RUNS.md` extended without a single deletion.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator review of the Phase 1 report and the two findings</name>
  <files>experiments/LEADERBOARD.md, experiments/RUNS.md, experiments/baselines/leaderboard.json</files>
  <read_first>
    - `experiments/LEADERBOARD.md` (as generated in Task 2)
    - `experiments/RUNS.md` (the new pointer section added in Task 2)
    - `.planning/phases/01-measurement-rig-core/01-CONTEXT.md` § "Specific Ideas" — why the rig must be able to say "no"
  </read_first>
  <action>
    Present the generated report to the operator and pause. Claude performs no
    further edits in this task: everything verifiable was already automated in Tasks
    1 and 2, and this checkpoint exists only for the two judgement calls that are
    the operator's — accepting the two measured findings, and accepting the report's
    stated assumptions as the adjudication rules for Phases 3 through 5.

    In the message that opens the checkpoint, state the measured `ΔTS(A, B)` and
    `ΔTS(A, C)` with their 95% CI, permutation p, Holm-adjusted p, MDD, corrected
    delta and verdict, and say plainly whether either cleared the ≥0.01 corrected
    floor. Do not characterise either outcome as expected or unexpected — D-03
    forbids the rig from having an expectation.

    On "approved", record both findings verbatim in the plan summary and in
    `.planning/STATE.md`'s Accumulated Context, and close the phase. On any other
    response, apply exactly the requested change to the report or to the recorded
    findings, regenerate the artifacts, re-verify the reproducibility gate, and
    re-present.
  </action>
  <what-built>
    Claude has already run all three evaluations, committed the three baseline
    records, regenerated `experiments/baselines/leaderboard.json` and
    `experiments/LEADERBOARD.md`, added the additive pointer section to
    `experiments/RUNS.md`, and verified that re-running the adjudication is a
    byte-level no-op. Nothing below asks you to run anything Claude could have run.
    This checkpoint exists because two judgement calls in this phase are yours: the
    measured A-vs-B and A-vs-C findings are the first real evidence the rig has
    produced, and Phases 3 through 5 will be adjudicated by the rules this report
    states.
  </what-built>
  <how-to-verify>
    1. Open `experiments/LEADERBOARD.md`. Confirm the `## How to read this report`
       block is present and that its four points read as deliberate statistical
       choices rather than as errors: per-bucket sigma from the bucket's own p
       (`0.094868` at n=10, not MEAS-09's illustrative `0.086`); the winner's-curse
       sigma-hat being the paired-difference SE and therefore roughly an order of
       magnitude below the `0.019` figure quoted in `PROJECT.md`; the achievable MDD
       at n=200; and the deliberate absence of per-scenario Holm correction.
    2. In the `## Candidates` table, confirm the ordering is by TechnicalScore
       descending and that no row's position is explained by its HR@10.
    3. In the `## Per-scenario breakout` table, confirm the Boundary (n=10) and
       Intent Override (n=30) rows are flagged as not decision-grade.
    4. In the `## Pairwise adjudication` table, read the two measured rows. Confirm
       each states its MDD, its sigma-hat, its k and its `E[max of k]`, and that you
       can re-derive `corrected delta` from `delta - sigma-hat * E[max of k]` by
       hand. This is the number Phase 5 will compare against the ~0.005 stopping
       threshold.
    5. Read the new pointer section in `experiments/RUNS.md`. Confirm it added
       content and deleted nothing, and that the two findings are stated as
       measurements rather than as expectations.
    6. Decide whether the two measured findings change anything about the Phase 3/4
       plan — in particular, if `ΔTS(A, B)` came out small, confirm you are content
       that the guaranteed true-positive control for this rig is the synthetic
       `promote_hits_to_rank_one` fixture rather than run B.
  </how-to-verify>
  <acceptance_criteria>
    - The checkpoint message quotes both measured deltas with CI, permutation p, Holm-adjusted p, MDD, corrected delta and verdict, and neither is described as expected or unexpected.
    - `experiments/LEADERBOARD.md` opens in a reader and contains all five required headings.
    - The operator returns either "approved" or a specific requested change.
    - On approval, both findings appear verbatim in `.planning/phases/01-measurement-rig-core/01-09-SUMMARY.md` and in `.planning/STATE.md`'s Accumulated Context.
  </acceptance_criteria>
  <verify>
    <human-check>Report reviewed; the two findings and the stated-assumptions block are accepted as the adjudication rules for Phases 3 through 5.</human-check>
  </verify>
  <resume-signal>Type "approved" to close Phase 1, or describe what should change in the report or in the recorded findings.</resume-signal>
  <done>The measurement instrument is accepted and the rules it states are the rules the bake-off will be judged by.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evaluation run → committed evidence | These three records become the permanent baseline every later phase compares against; a record whose configuration is misdeclared poisons every downstream comparison |
| measured outcome → recorded finding | An expectation written up as a measurement would be the most damaging possible failure for a project whose central claim is statistical honesty |
| `experiments/RUNS.md` prose evidence | The file holds irreplaceable verification history that a careless rewrite would destroy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | run record declaring a configuration that was not applied | mitigate | `overrides` in each `summary.json` is asserted to equal the exact flags used, and the fingerprint is derived from those same overrides after `spec.validate()`; the three fingerprints must be pairwise distinct |
| T-01-15 | Repudiation | asserting an outcome for a run whose true effect is unknown | mitigate | D-03 extended to run B per the orchestrator's adjudication of RESEARCH Q1; acceptance criteria assert internal consistency (CI contains delta, MDD identity, verdict/`failed_criteria` equivalence) and byte-reproducibility, never a magnitude or a significance outcome |
| T-01-21 | Repudiation | destruction of `experiments/RUNS.md` prose evidence | mitigate | D-05 makes the edit additive only; `git diff --numstat -- experiments/RUNS.md` must report zero deletions |
| T-01-04 | Repudiation | non-reproducible published verdict | mitigate | The adjudication is re-run and `git diff --quiet` on both leaderboard artifacts must exit 0 |
| T-01-05 | Repudiation | the three records being ignored by git | mitigate | `git check-ignore -q` on each `summary.json` must exit 1 |
| T-01-19 | Denial of Service | a long run mistaken for a hang and killed mid-evaluation | mitigate | Run B is budgeted generously with a documented reason (superseded-HEAD FTS runs took 747-800 s); a background invocation is prescribed instead of shortening the run; the atomic publish means a killed run leaves no partial record |
| T-01-03 | Information disclosure | ground truth reaching the Agent during a real run | mitigate | Enforced structurally by plan 01-08's `_SampleMappingAgent` and its test; unchanged here |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed; `dependencies = []` |
</threat_model>

<verification>
- Run A reproduces `0.92 / 0.524466 / 3.425 / 0.7575 / 0.76884` and agrees with `anchor-legacy` on every aggregate and every scenario summary.
- Three baseline records committed and tracked; `git check-ignore` exits 1 on each.
- The adjudication produces two internally consistent rows and re-running it is a byte-level no-op.
- `git diff --numstat -- experiments/RUNS.md` reports zero deletions.
- `uv run python -W error::ResourceWarning -m unittest -v` passes the full suite.
</verification>

<success_criteria>
- MEAS-16 satisfied: the statistics engine is validated against the retained historical row, by two independent code paths, before any new candidate exists.
- ROADMAP Success Criterion 3 satisfied: the paired bootstrap and permutation, Holm correction, practical floor and winner's-curse correction produce a reproducible verdict and an MDD over retained rows, using no new candidates.
- Three provenance-carrying candidate records are permanently committed.
- `experiments/RUNS.md` gains a pointer and loses nothing.
- The operator has accepted the report's stated assumptions as the adjudication rules for Phases 3 through 5.
</success_criteria>

<output>
Create `.planning/phases/01-measurement-rig-core/01-09-SUMMARY.md` when done.
</output>
